# CageMaster4 下一阶段开发方向分析

> 依据：源码通读（core / renderer / ui / expert / story / data）+ `reports/` 既有分析（教学有效性、FunScore、关卡校验、手册对齐、VO 报告）
> 时间基准：2026-08

---

## 一、现状评估

### 1.1 已完成的硬成果（来自既有报告）
| 维度 | 状态 | 证据 |
|---|---|---|
| 关卡可解性 | **51/51 通过**校验，0 失败 | `code-manual-alignment-report.md`（validate 结果） |
| 手册-代码一致性 | **100% 对齐（手册 v1.4）** | 同上，237 API 双向对照 |
| 教学系统有效性 | **92%** 通过（6/8 警告 2） | `summary-report.md` |
| P0 接入 | BattleManager 接入主链路、assets/audio 填充（1305 文件）、音量同步均完成 | `code-manual-alignment-report.md` |
| P1 接入 | HintSystem 接入、WhatIfManager 插槽完成 | 同上 |
| AI 解题验证 | 专家 LLM 跑 101–109，**77.8% 通过**、2 超时（推理链偏长导致解析失败，非逻辑错） | `llm-expert-report.json` |

### 1.2 架构优势（应保持）
- **分层清晰**：`core/`（纯逻辑、无 DOM）↔ `renderer/`（Canvas 2D）↔ `ui/`（GameApp 编排）↔ `expert/`（提示/教学）↔ `story/`（叙事）↔ `data/`，依赖方向严格单向，便于测试与复用。
- **双环境兼容**：`headless-engine.js` / `battle-manager.js` 经 `eval` + 全局 `Board/TechRater` 同时支持 Node 与浏览器，测试脚本可脱离浏览器跑。
- **求解引擎质量高**：`TechRater` 用 bitmask 候选集，覆盖 4×4/6×6/9×9 与 10 级技巧，采用「最低可用技巧」策略，并产出影响力评分、三阶段剧本、难度评级——这是项目的核心护城河。

### 1.3 关键短板（来自数据，非臆测）
1. **Boss 战趣味度极低**：`funscore-analysis.json` 显示 FunScore 总分仅 **11 / 19 / 19**，`tempo=0`、`risk=0`（climax 38–63 尚可）。即：有高潮但**无节奏张力、无风险压力**。熟练方（ying）胜率 87.5%、普通方（avg）50%，但「赢了也没紧张感」。
2. **教学引导采纳率低**：`summary-report.md` 指出 `semiAuto` 引导仅被采纳 **33%**，玩家偏离引导路径；共性问题 `watch-cell-missed`（3 关）。玩家不跟着学，教学有效性打折。
3. **配音（VO）整体暂缓**：`story-engine.js` 中 `VOICE_ENABLED=false`；`莹莹配音台词问题分析报告` 指出约 187 条 VO 台词需**阿拉伯数字转汉字**（"112"→"一百一十二"、"2×2"→"二乘二"）、× 符号与标点美化——叙事沉浸感未兑现。
4. **美术占位**：`gallery-panel` 等用 **emoji 占位**，33 张 CG / 49 个 Q 版 chibi 资源已在 `assets/` 但**未接入显示机制**。
5. **Boss 高级机制未激活**：`board.js` 已支持 `lockCells/fakeCells/regionLocks/cageCollapse`，但 `code-manual-alignment-report.md` 遗留建议指出——各章 Boss 关 JSON **尚未补充这些字段**，机制等于空置。
6. **无构建/打包系统**：`package.json` 仅含 migrate/test:ai/validate，靠本地服务器运行；`core/*.js` 用 `eval` 加载属临时方案，生产化需打包。

---

## 二、下一阶段路线图（优先级建议）

### 🔴 P0（直接决定「好不好玩 / 完不完整」，建议本阶段就做）

**P0-1 · Boss 战趣味度工程（FunScore tempo/risk 补完）**
- 现状：tempo=0、risk=0。
- 动作：
  - 引入**节奏维度**：限时压力 / 连击倍率 / 「对手即将填满」的威胁条，让 FunScore 的 tempo 有真实来源。
  - 引入**风险维度**：可被扣血/扣时的必杀技、赌注式「假格」「幽灵格」、错误重试代价，让 risk 非零。
  - 在 `BattleManager` 事件流中**埋点采集** tempo/risk/climax，把 FunScore 变成可回归的量化指标，每个 Boss 关目标 FunScore ≥ 50。
- 验收：`funscore-*.json` 复测，tempo、risk 均 >0，总分均值翻倍以上。

**P0-2 · 教学引导「锁得准、讲得懂」**
- 现状：semiAuto 采纳率仅 33%，`watch-cell-missed` 跨 3 关。
- 动作：
  - 对 `watchCells` 实施**软锁定/强高亮**（偏离时给明确提示而非放任），参考 `canSelectCell` 已具备的选中拦截能力做扩展。
  - `watchCells` 优先选**单候选格**（报告建议候选数 2.0 → 选单候选，降低认知负担）。
  - 提示动画与讲解气泡强绑定（代码里已修的 `applyHintAction` text 透传，需回归确认全技巧覆盖）。

**P0-3 · 配音（VO）上线**
- 动作：
  - 先消 `莹莹配音台词问题分析报告` 的 P0：**数字转汉字**（含 "112"→"一百一十二"、"2×2"→"二乘二"）、标点美化（"="→"就是"、引号转义）。
  - 开启 `VOICE_ENABLED`，按 `voice_index.json`（1227 文件 / 12 角色）接 `audio-service` 的 voice 通道。
  - 三色提示角色（阿妍/守笼人/莹莹）的语音权重与文字同步。
- 验收：主角沈墨线 + 三提示角色 VO 全覆盖、无数字直读。

**P0-4 · 美术资产真接入（去 emoji 占位）**
- 动作：`gallery-panel`、立绘、CG（33）、chibi（49）接入 `assets/` 既有资源；`book-shell`、Boss 立绘用真实图替换 emoji。

### 🟠 P1（完善度与可持续性）

**P1-1 · 激活 Boss 高级机制**：为 109/208/307/406/506/606/706/801 在关卡 JSON 补 `lockCells/fakeCells/regionLocks/cageCollapse`，让 `BattleManager` 的锁格/假格/笼崩塌真正生效（字段解析已就绪，缺数据）。

**P1-2 · 错误日志持久化**：迁移 V3 `error-reporter`，便于线上问题定位（当前仅 console.warn）。

**P1-3 · ScriptData 多语言文案填充**：`t(key,params)` 接口已就绪，补 `zh-CN` 全量文案，预留 `en` 骨架。

**P1-4 · 移动端打磨**：拖拽多选、长按同数高亮、假设抽屉在小屏的布局；`PerformanceMonitor` 低端机降级策略验证。

**P1-5 · 关卡内容扩容**：当前仅 51 关（7 章 + 隐藏），第 5–7 章偏薄。结合 `LevelPoolManager` 的专家池，扩充中后段内容，并增加更丰富的 `lessonPlan` 覆盖（validate 报 53 个「缺 lessonPlan」警告，多为 Boss/隐藏关，可控）。

### 🟢 P2（工程化与长线）

**P2-1 · 引入构建/打包**（Vite）：消除 `core/*.js` 的 `eval` 全局加载 hack，产出可部署静态包，提升首屏与可维护性。

**P2-2 · 测试体系加固**：`test:ai` 已有；补 `hint-system` / `TechRater` / `BattleManager` 的回归用例，把 FunScore、教学采纳率纳入 CI 看板。

**P2-3 · 复盘/教学可视化（strategy-replay）**：`strategy-replay.js` + `TechRater.getSteps()` 已能产出最优路径，做「玩家路径 vs 最优路径」对比回放，强化学习反馈。

**P2-4 · 数据驱动调参**：把 `reports/` 的 100+ 份实验（novice/expert/impatient、funscore、A/B fake-note、observer）沉淀为可调参数表，形成「改一版 → 跑实验 → 看报告」闭环。

---

## 三、优先级排序（建议执行顺序）

```
P0-1 Boss 趣味度(量化)  → P0-2 教学锁定采纳 → P0-3 VO 上线 → P0-4 美术接入
        ↓
P1-1 Boss 机制激活 → P1-2 错误日志 → P1-3 多语言 → P1-4 移动端 → P1-5 关卡扩容
        ↓
P2-1 打包构建 → P2-2 测试/CI → P2-3 复盘可视化 → P2-4 数据闭环
```

**理由**：P0 直接决定「游戏是否成立、是否完整」——Boss 没张力、没声音、没美术，再好的求解器也只是 demo；先量化 FunScore 目标再动手，避免凭感觉调参。P1 把已有但未兑现的能力（Boss 字段、日志、多语言）落地。P2 是工程债与长线运营。

---

## 四、风险与提醒

1. **FunScore 指标需先定义再优化**：当前 tempo/risk 为 0 可能是「没采集」而非「真没发生」。先确认埋点，再判是否真缺机制——避免盲目加系统。
2. **eval + 全局加载是技术债**：短期能跑，长期阻碍打包与 Tree-shaking；P2-1 应尽早排期，否则资产/代码越堆越难迁。
3. **VO 数字转写是人工+规则活**：报告已给出清晰优先级（数字>×>标点），建议先做规则脚本批量转写再人工校对，别逐条手改。
4. **教学采纳率 vs 自由度权衡**：强锁定能拉高采纳率，但会牺牲探索乐趣；P0-2 建议「软锁定+强提示」而非硬锁死。
5. **现有 manuals 同步**：`cagemaster4-game-manual` / `cagemaster4-dev-manual` 为 HTML 手册（v1.4 已代码对齐）。任何 API/玩法变更须同步这两份手册，避免再次漂移（本次分析已发现并修正 3 处 API 描述错误）。

---

## 五、一句话结论

> CageMaster4 的**求解内核与教学框架已是强项**（51/51 可解、92% 教学有效、手册对齐），下一阶段应从「能玩」转向「好玩且完整」：**量化并补齐 Boss 战的节奏与风险、让配音与美术真正落地、把已就绪却空置的 Boss 机制接上数据**——用 `reports/` 已有的实验体系做闭环验证。
