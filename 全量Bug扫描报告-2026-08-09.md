# CageMaster4 全量剧本/关卡 Bug 扫描报告
> 扫描时间：2026-08-09 ｜ 范围：`data/levels/*.json`（51 关）+ `data/scripts/scripts.json`（3 周期/55 引用关卡）+ `story/story-engine.js`（情绪/立绘/站位表）
> 方法：① 内置 `validate-levels.js` 字段校验 ② 自研 `scripts/bugscan.mjs` 深度扫描（情绪/说话人/对话结构/教学重复/关卡断层/叙事定位/伏笔关键词）③ 定向 grep 跨路线时间线冲突

## 总览（按 8 维度）
| 维度 | 发现数 | 高 | 中 | 低 | 结论 |
|---|---|---|---|---|---|
| ① 字段合法性 | 16(警告) | 0 | 0 | 16 | 51/51 通过，0 硬错 |
| ② 对话结构一致性 | 0 | 0 | 0 | 0 | 全部条目含 speaker/text，无结构破损 |
| ③ 参数缺失 | 21 | 7 | 14 | 0 | 14 关空 guided.hintText + 7 关缺 boardData |
| ④ 文案前后伏笔矛盾 | — | 1 | 2 | 0 | **二/三周目路线根本未导入**，已知冲突无法在游戏内验证 |
| ⑤ 情绪字段错误 | 17 | 0 | 0 | 17 | 多来自我导入的序章（轻声/低语/思考·回忆等未登记） |
| ⑥ 叙事定位冲突 | 1 | 0 | 0 | 1 | 薇拉在 PORTRAIT_MAP 有立绘但缺 CHAR_SIDE |
| ⑦ 教学文案重复 | 16 | 0 | 15 | 1 | 星衡法则短语跨 8 关重复；照片/双曜场景跨周目重复 |
| ⑧ 关卡逻辑断层 | 7 | 7 | 0 | 0 | 6 个章末关 + 最终 Boss(801) 有剧本无盘面 |

---

## 一、字段合法性（内置校验器）
【位置】`data/levels/*.json`（51 文件）
【风险】低（全部通过，无硬错）
【修复】维持现状即可。16 条仅为警告：
- `phases.guided.hintText 为空` ×14：L104, L201–L208, L307, L406, L506, L606, L706
- `phases.demo.steps[x] cage 0 不存在` ×2：L107(step0)、L108(step3) —— demo 演示步骤指向不存在的笼 id

---

## 二、对话结构一致性
【位置】`scripts.json` 全部 preDialog/clearDialog/prologue/epilogue
【风险】无
【结论】所有对话条目均为 `{speaker,text,emotion}` 合法对象或纯文本旁白；无缺 speaker/text、无类型错乱、无重复 key。`沈墨 CHIBI`/`苏晚` 等 token 均能被 `SPEAKER_ALIASES` 正确解析。

---

## 三、参数缺失
**3-1【位置】14 个 level JSON 的 `phases.guided.hintText` 为空（见上）【风险】中【修复】补一句定制化引导语，例如 L104 引导填某格的提示。**
**3-2【位置】7 个被剧本引用但无 boardData 的关卡（详见第八节）【风险】高【修复】补建 level-*.json 或移除剧本引用。**

---

## 四、文案前后伏笔矛盾（重点）
**4-1【位置】`scripts.json` 周期结构【风险】高【现象】**
- cycle 1（沈墨）：7 章 55 关，内容完整。
- **cycle 2（薇拉）：仅 1 关**（ch0=0, ch1=1, ch2=0）。
- **cycle 3（伊藤）：0 关**（ch0/1/5/6/7 全空）。
- 跨路线关键词 grep（伊藤/断电/截断/发报/12月/窗台/桥洞/铁盒/闭店/登船）命中 **0 次**。
【结论】「三岔口=三视角共用谜题」的核心设定在**实际游戏数据中完全未落地**，目前只交付了沈墨单线。之前在大纲 txt 中定案的跨路线冲突（伊藤截断电源线应改 12.7 深夜、702 纸条归伊藤、509 铁盒纸条指向发报机权限题）**仅存在于大纲，未进入 scripts.json**，故无法在游戏内验证，导入二/三周目时必须严格对齐这些定案。
【修复】将二/三周目大纲按已定案冲突逐章导入；导入前以 `主关卡登记表.md` 为基准，先补建缺失 boardData 再填对话。

**4-2【位置】跨周目相同叙事文本（见第七节 L305/L504、L306/L502、L307/L503、L403/L702、L404/L703）【风险】中【现象】同一段「照片/航线/双曜/三子法」描写被原样复制到不同周目关卡。**
【修复】若是有意「共享记忆」可保留，但建议为每个 POV 增加差异化细节（如薇拉视角的照片来源、伊藤视角的铁盒触发），避免玩家三周目产生「复制粘贴」感。

**4-3【位置】苏晚定位【风险】中(已收敛)【现象】扫描确认 `suwan` 仅出现在 cycle 1，未越界到二/三周目，符合设定；但苏晚在二/三周目大纲中仍无收口（见 4-1）。**

---

## 五、情绪字段错误（含我导入引入的回归）
【位置】全部 17 处集中在 `cyc1/ch1`（序章·信 L101–L109 及章节序幕）
【风险】低（不崩溃，仅静默回退默认立绘+常规语速）
【现象】以下 emotion 不在 `EMOTION_ZH_MAP`，被引擎回退：
- `轻声`、`低语`、`思考·回忆`、`回忆·低语`、`温和·留声`、`温和·教学`、`温和·展望`、`思考`
- 其中 `轻声/低语/思考·回忆/回忆·低语/温和·留声/温和·教学/温和·展望/思考` **来自本次导入的序章**（我引入的回归）；`温和·*` 部分亦出现在章节序幕。
【精准修复】在 `story-engine.js` 的 `EMOTION_ZH_MAP` 补映射（均指向已有英文立绘 key，零新增资源）：
```
"轻声":"calm", "低语":"calm", "思考":"think", "思考·回忆":"think",
"回忆·低语":"sad", "温和·留声":"calm", "温和·教学":"teaching", "温和·展望":"calm"
```
> 注：`teaching` 已存在 map 但非立绘 key，会回退 default 立绘（可接受，教学态本就用默认立绘）。

---

## 六、叙事定位冲突
**6-1【位置】`story-engine.js` CHAR_SIDE【风险】低【现象】`vera`（薇拉）在 `PORTRAIT_MAP` 有立绘，但**不在 `CHAR_SIDE`**。引擎 line 1226 缺省回退 `'right'`（恰好正确），故不崩，但与其他发言角色登记不一致，属维护隐患。
【修复】在 `CHAR_SIDE` 增加 `'vera': 'right'`。
**6-2【位置】整体【风险】高(见 4-1)【现象】二/三周目路线未导入，叙事定位整体缺失。**

---

## 七、教学文案重复
【位置/风险/修复】如下（重复文案建议改为每关定制）：
1. **中** `再用星衡法则找两个格子。` 跨 **L201–L208 共 8 关** 完全重复 → 改为各关具体坐标引导（如「再用星衡法则确定 c4、e7」）。
2. **中** `正确！继续用星衡法则推进。` 跨 L204/L206/L207 → 同上。
3. **中** `用同样的方法，再找出两个数字。` 跨 L101/L107（序章内重复，我导入引入）→ 改为具体格。
4. **中** 「是她。她走之前，把这张照片留在了这里…」「沈墨把照片翻过来。背面有一行字：1941年11月15日…」「她改了航线。她走的时候…」跨 **L305/L504** 原样重复 → 见 4-2 差异化。
5. **中** 「沈墨在练习册的页脚发现了另一行字…11月15日」跨 **L306/L502** 重复 → 见 4-2。
6. **中** 「她解了一半，然后停下了…」跨 **L307/L503** 重复 → 见 4-2。
7. **中** 「答对了！双曜的意义不在填数，而在排除…」「继续用「逐对扫描」的方法找双曜…」跨 **L403/L702** 重复 → 周目间差异化。
8. **中** 「继续寻找三子法结构…」跨 **L404/L703** 重复 → 周目间差异化。
9. **低** 模板化短语（「再用星衡法则找两个格子」「用同样的方法，再…」「继续用…」「综合运用本章全部技巧」）在 ≥3 关出现 → 批量替换为具体数字/坐标引导。

---

## 八、关卡逻辑断层
【位置】`scripts.json` 引用但 `data/levels/` 无对应文件【风险】高
| 关卡 | 周期/章 | 标题 | 状态 |
|---|---|---|---|
| L209 | cyc1/ch2 | 章末考核 | 有剧本无盘面 |
| L299 | cyc1/ch2 | 第2章结尾·走廊 | 有剧本无盘面 |
| L399 | cyc1/ch3 | 第3章结尾·决裂的回声 | 有剧本无盘面 |
| L499 | cyc1/ch4 | 第4章结尾·光从裂缝中透出 | 有剧本无盘面 |
| L599 | cyc1/ch5 | 第5章结尾·抉择的门槛 | 有剧本无盘面 |
| L699 | cyc1/ch6 | 第6章结尾·（第一周目普通结局）·新守笼人 | 有剧本无盘面 |
| L801 | — | 最终 Boss（仅 BOSS_LEVEL_MAP 引用） | **剧本、盘面皆无** |

【现象】沈墨单线本身就有 6 个「章末/章尾」关卡无 boardData，玩家打到章末会卡在没有盘面的关卡；最终 Boss 801 在 `core/battle-manager.js` 的 BOSS_LEVEL_MAP 中被引用，但 scripts.json 与 data/levels 均无对应数据。
【精准修复】按 `主关卡登记表.md` 补建这 7 个 level-*.json（含 gridSize/boardData/solution/cages/lessonPlan）；L801 还需补脚本对话与 Boss 配置。

---

## 修复优先级建议
1. **P0（阻断可玩性）**：补建 L209/299/399/499/599/699/801 的 boardData（第八节）——否则章末与终局无法进入。
2. **P1（我方回归，顺手修）**：`EMOTION_ZH_MAP` 补 8 条序章情绪映射（第五节）+ `CHAR_SIDE` 补 `vera`（第六节）——改 1 个文件即可。
3. **P1（结构）**：按已定案冲突导入二/三周目（第四节），否则「三岔口」卖点不成立。
4. **P2（体验）**：去重教学文案（第七节）、填充 14 个空 guided.hintText（第三节）、修复 L107/L108 demo cage 引用（第一节）。

> 附：本扫描产生 `scripts/bugscan-output.json`（全量结构化 findings）与 `scripts/bugscan.mjs`（可复跑）。
