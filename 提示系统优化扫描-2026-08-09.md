# 提示系统优化扫描 — CageMaster4
> 扫描时间：2026-08-09 ｜ 对象：`expert/hint-system.js`(1340行) + `expert/expression/character-templates.js` + `renderer/hint-adapter.js` + 51 个 level JSON 的 hint 数据
> 结论：**提示引擎本身成熟（三级渐进 / 证据链归一 / 模板全覆盖 / 314 个 popup 结构合法），无明显崩溃级缺陷。优化空间在「体验与叙事一致性」层面。**

## 系统现状（已验证的良好基础）
- 三级渐进提示：L1 模糊方向 → L2 报技巧名 → L3 用 `renderHint` 模板报具体数字/笼和。
- `character-templates.js` 对 **10 种技巧 × 3 角色** 均有具体模板（覆盖 nakedSingle…swordfish）。
- 证据归一化完整（`_getPortrait...` 同级）：regionType/regionIndex/excludedNumbers/eliminatedPositions 全部补齐，红叉动画不丢。
- 数据面：guided.hintText 37/51 有值、successText 51/51、semiAuto 51/51 启用、popup_hint 314 个且 **cell 引用 0 越界**。

---

## 优化点（按影响力排序）

### 【P0-高】1. 提示角色与叙事路线完全脱节
【位置】`expert/hint-system.js:34` `HINT_CHARACTERS = [ayan(0.6), cagekeeper(0.3), ying(0.1)]`
【问题】提示只可能来自 **阿妍 / 守笼人 / 莹莹**，三人都不是任何路线的 POV。当前序章（苏晚在场）里点提示，出来的却是阿妍/莹莹的声音；**苏晚、薇拉、伊藤、沈墨均不可作提示角色**，破坏沉浸。
【精准修复】
- 把 `HINT_CHARACTERS` 改为「路由感知」：依据当前 cycle/章节选用 POV 角色（序章→沈墨/苏晚，薇拉线→薇拉，伊藤线→伊藤），保留 阿妍/守笼人 作 fallback。
- 在 `character-templates.js` 的 `CHARACTER_TEMPLATES` 为新增 POV 角色补 `hint` 模板（可复用 ayan 的 10 技巧模板做初版）。
- 建议新增 `HintSystem.setRouteCharacter(charId)` 由 `story-orchestrator` 进入关卡时调用。

### 【P0-高】2. Level-3 精准提示的静默降级风险
【位置】`expert/hint-system.js:1042` `_tryTemplateDialogue` + `character-templates.js:97` `renderHint`
【问题】模板含 `{num}/{sum}/{placed}` 占位符；若 `deduction.evidence` 缺这些字段，渲染出 `?` → 第 1051 行判定回退为笼统旧文案。**精准报数能力完全依赖 TechRater 证据完整性**，一旦某技巧证据缺字段，玩家点满三级仍只得到"用排除法推一下"。
【精准修复】
- 在 `getHint()` 组装 `evidence/ctx` 时，用 `this.solution[cell.row][cell.col]` **直接回填 `num` 兜底**（系统本就持有 solution），保证 Level-3 永远能报出目标数字。
- 加一行日志/埋点统计实际回退率，定位哪些技巧证据缺失。

### 【P1-中】3. 教学引导文案重复（前次全量扫描已确认）
【位置】`data/levels/*.json` 的 `lessonPlan.phases.guided.hintText`
【问题】`再用星衡法则找两个格子。` 在 **L201–L208 八关完全重复**；照片/双曜/三子法场景被原样复制到跨周目关卡（L305/L504、L403/L702、L404/L703）。
【精准修复】逐关改为具体坐标引导（如「再用星衡法则确定 c4、e7」）；跨周目相同记忆文本改为各 POV 差异化细节。

### 【P1-中】4. 14 关引导阶段无提示语
【位置】`guided.hintText` 为空：L104, L201–L208, L307, L406, L506, L606, L706（与内置校验器一致）
【问题】引导(guided)阶段本应给一句针对当前格的提示，这些关却是空字符串，玩家在引导关也得不到方向。
【精准修复】为每关补一句指向 `targetCell` 的具体引导（可参考同章其他关风格）。

### 【P1-中】5. popup_hint / demo 引用完整性缺自动校验
【位置】`data/levels/*.json` 的 `popup_hint` 与 `phases.demo.steps`
【问题】已验证 popup cell **0 越界**，但 cage 引用尚未全量校验；内置校验器已发现 `L107/L108` 的 demo step 指向 **不存在的 cage 0**。说明"引用类提示"存在断链隐患。
【精准修复】在 `scripts/validate-levels.js` 增加一条校验：扫描所有 `popup_hint.target`(cageId/cell) 与 `demo.steps[].target` 是否真实存在于 `cages`/`boardData`，缺则报 warning。

### 【P2-低】6. Level-1 提示过笼统
【位置】`expert/hint-system.js:1058` `_getLevel1Dialogue`
【问题】L1 仅"看看第N行/列/宫"，不预告任何技巧方向，新手难建立预期。
【精准修复】L1 末尾追加一句技巧名预告（仍不报答案），如"这一行里有个数字快藏不住了"。

### 【P2-低】7. hintVoiceId 13 关缺失
【位置】51 关中 29 已填、13 空/null
【问题】当前 `VOICE_ENABLED=false`（VO 暂缓），不影响；但一旦开启语音，这 13 关会静音。
【精准修复】补齐 13 关的 hintVoiceId，或将缺失标记 `optional` 以免报错。

### 【P2-低】8. 体验细节（可选）
- **角色选择纯随机**（`_selectCharacter` 按权重 random），无"提示伙伴"连续性；可固定每局一个伙伴建立熟悉感。
- **冷却固定 5s**（`cooldownMs=5000`）；可改为自适应：卡关越久冷却越短，刚填对后稍长，避免打扰。

---

## 落地优先级
1. **P0**：路由感知提示角色（#1）+ Level-3 数字兜底（#2）——两项改 1 个引擎文件 + 扩模板，直接消除"提示声音出戏"和"点满三级仍笼统"两大体验硬伤。
2. **P1**：去重教学文案（#3）、补 14 关空 hintText（#4）、加引用完整性校验（#5）。
3. **P2**：L1 预告（#6）、补 voiceId（#7）、体验打磨（#8）。

> 复跑脚本：可直接对 `scripts/bugscan.mjs` 增加 hint 维度，或新增 `scripts/hint-audit.mjs`。
