# 笼中密码 · CageMaster4 游戏说明书

> 版本对应：代码仓库 `cagemaster4`（V4 里程碑，2026-08）
> 适用对象：新玩家 / 想系统了解玩法与功能的玩家
> 配套文档：本仓库 `cagemaster4-game-manual/`（交互式 HTML 图文手册）、`cagemaster4-dev-manual/`（开发手册）

---

## 1. 游戏概述

**《笼中密码》** 是一款以「杀手数独（Killer Sudoku）」为核心玩法、叠加谍战叙事与 Boss 对决的网页解谜游戏。

- **核心循环**：你面对一张被分割为若干「笼子（Cage）」的数独盘，每个笼子标注了**数字之和**。你需要同时满足**数独基本规则**与**笼子和值约束**，把整盘填满。
- **叙事外壳**：1940 年代上海租界谍战。主角是代号「沈墨」的潜伏者，在破解密码的过程中逐步揭开身世与旧案线索。
- **AI 陪练**：三位角色（阿妍 / 守笼人 / 莹莹）会在你卡住时给出**三级渐进提示**，并实时讲解推理。
- **Boss 战**：每章末关是一场与 AI 对手同盘竞速的「笼局对决」，比谁先填满关键据点。

> 技术侧一句话：纯前端 HTML + Canvas 2D，无需联网即可游玩（关卡与剧情数据本地加载）。

---

## 2. 核心规则

### 2.1 数独基本规则
- 每张盘是 `N×N` 方格（入门 4×4，正式 9×9）。
- 每一**行**、每一**列**、每一个 **宫（3×3 方块）** 内，数字 `1~N` **互不重复**。

### 2.2 笼子（Cage）与和值
- 盘面被若干粗线分割成**笼子**，每个笼子有一个**小数字 = 该笼所有格的数字之和**。
- 笼子内的数字也**互不重复**（与行/列/宫规则叠加）。
- 这是杀手数独区别于普通数独的关键：**和值**提供了普通数独没有的额外推理线索。
- 部分关卡支持**嵌套笼**（一格属于多个笼子），推理时需要同时满足多个和值。

### 2.3 星衡法则（Rule 45）
- 在 9×9 盘中，**任意一行/列/宫的数字总和 = 45**。
- 当某个区域（行/列/宫）与若干笼子相交时，可用「内突（Innie）/ 外突（Outie）」技巧直接推出某格的值——这是中高阶关卡的破局利器。

### 2.4 完成判定
- 当所有格子填对、且冲突数为 0 时本关通关。
- 填错的数字会标红（`isError`）；与标准答案不符但不会立即判负，可在「错误限制模式」下被约束（见 §7）。

---

## 3. 操作指南（键鼠 / 触屏通用）

### 3.1 基础操作

| 操作 | 鼠标 / 触屏 | 键盘 |
|---|---|---|
| 选中格子 | 点击棋盘格 | — |
| 填入数字 | 选中后点底部数字盘 | 数字键 `1`–`9` |
| 切换笔记（候选数）模式 | 点「笔记」按钮 | `N` |
| 在笔记模式记/擦候选数 | 选中后点数字盘 | 数字键 `1`–`9` |
| 擦除（数字+笔记） | 点「橡皮」按钮 | `Backspace` / `Delete` |
| 高亮所有同数字格 | 长按数字键 / 点已填格 | — |
| 书签当前关 | — | `B` |
| 关闭弹窗 / 取消选中 | `Esc`（依次：取消数字笔 → 退出批量笔记 → 关抽屉） | `Esc` |

### 3.2 划选 / 批量
- **拖拽选格**：在棋盘上按住拖动可多选若干格。
- 拖完若选中**多格**，自动进入**批量笔记模式**，按数字键可一次性给所有选中格记同一候选数。
- **绝杀连线**：若拖拽划过全部 3 个据点核心格，会自动连线触发剧情「绝杀」演出。

### 3.3 假设模式（🧪 WhatIf）— 安全试错
当你不敢确定某步时，可进入假设分支验证思路，错了随时回退，不影响主盘：

| 操作 | 说明 | 键位 |
|---|---|---|
| 打开/收起抽屉 | 右侧「🧪 假设」皮扣浮条 | `W` 或 `D` |
| 新建快照 | 暂存当前盘面，开一层新假设 | `S`（仅假设激活时）|
| 采纳当前盘面 | 退出假设模式，保留填数 | `A` 或 `Enter`（仅假设激活时）|
| 回退到某快照 | 抽屉打开时 | `1` / `2` / `3` |
| 撤销一步 | 假设激活时替代擦除 | `Backspace` |

> 设计意图：正式落笔「不能错」的关卡（如 410 任务压力关），必须先开假设模式验证路径，错了回退，代价只在脑子里付。

### 3.4 提示系统（三级渐进）
- 点「提示」按钮（或对应入口）后，提示角色给出**三级由浅入深**的讲解：
  1. **方向**：点出该往哪里想（高亮目标区域）。
  2. **技巧**：点明用的是哪种数独技巧。
  3. **答案+证据链**：直接给出该填的数，并展示完整推理依据。
- 提示有 **5 秒冷却**，避免连续刷提示。
- 提示过程会锁定到目标格，点非目标格即自动放弃锁定、恢复正常操作。

### 3.5 辅助视图
- **热力图**：实时高亮「当前用最低级技巧就能推出」的格子，帮你发现可落子处。
- **雪崩自动补全**：当盘面上剩余空格**全部都是裸单（只剩一个数）**时，会自动一口气填完收尾。
- **45 账本**：把星衡法则的加减过程以「账本」形式可视化，降低高级技巧门槛。

---

## 4. 数独技巧图解（10 级推理阶梯）

游戏内置 `TechRater` 求解引擎，把推理拆成由易到难的 10 级技巧（外加「试数」）。理解它们，你就读懂了提示在讲什么。

| 级别 | 技巧名（代号） | 通俗解释 | 何时出现 |
|---|---|---|---|
| 1 | **孤星**（nakedSingle） | 某格只有 1 个候选数，直接填 | 全尺寸 |
| 2 | **唯一组合**（cageUnique） | 笼子和值 + 已填数，只剩一种数字分配 | 全尺寸 |
| 3 | **隐曜**（hiddenSingle） | 某数在某行/列/宫里只有 1 个能放的位置 | 全尺寸 |
| 4 | **星衡法则**（rule45） | 用行/列/宫总和 45 的内突/外突推出某格 | 9×9 核心 |
| 5 | **并蒂锁**（nakedPair） | 两格候选完全相同，可排他格清候选 | 6×6 / 9×9 |
| 6 | **双曜**（hiddenPair） | 两格独占两候选，清掉周边多余候选 | 9×9 |
| 7 | **区块排除**（pointingClaiming） | 宫指向行列 / 行列指向宫，排除他处 | 9×9 |
| 8 | **三子法**（nakedTriplet） | 三格候选互锁，清掉所在区域的其余候选 | 9×9 |
| 9 | **二连纵横阵**（xWing） | 数字在两行/两列形成矩形交叉排除 | 9×9 |
| 10 | **三才游鱼阵**（swordfish） | 三行/三列的鱼形交叉排除（X-Wing 进阶） | 9×9 |

> 引擎策略：始终取「**最低可用技巧**」而非「最快路径」，模拟人类逐步推理；提示与热力图都基于此。

---

## 5. 关卡、难度与进度

### 5.1 七章结构（含隐藏关）
1. **初识笼中密码**（101–109，隐藏 199）— 4×4 入门，建立基础规则
2. **四十五星衡**（201–208）— 引入星衡法则
3. **档案室深层**（301–307）— 剧情与技巧进阶
4. **尘封旧案**（401–406，隐藏 410）— 引入「不能错」任务压力关
5. **星辰梭核心**（501–506）— 高级技巧实战
6. **终局笼局**（601–606）— 收束主线
7. **秘术档案**（701–706，隐藏 799）— 终章挑战

> 另有 **专家挑战池**（`pool-*` 关卡）：按单一技巧（如 xWing、swordfish）专项训练，由 `LevelPoolManager` 顺序/随机派发。

### 5.2 难度评级
引擎会根据「最高技巧等级 × 技巧密度 × 剩余空格比例」给出 **1★–5★** 难度分，方便你挑合适关卡。

### 5.3 进度与解锁
- 通关一关即解锁下一关；章节首关随前一章进度解锁。
- 进度存储在本地（localStorage），重进自动续填（恢复本关已填进度）。
- 支持 `?level=301` 形式的 URL 跳关（需已解锁）。

### 5.4 Boss 战（笼局对决）
每章末关是一场 **Boss 战**：你与 AI 对手（如「守笼人·yan」）**同盘竞速**，比谁先填满关键据点。
- AI 同样基于 `TechRater` 推理决策，会放「必杀技」、加速、锁格、假格/幽灵格。
- 胜：复用正常通关流程；负：自动重玩该关。

---

## 6. 剧情与角色

- **沈墨**（主角，立绘在**左侧且翻转**）：潜伏于租界的密码破译者，身世与东余杭路 94 号母亲线索、符拉迪沃斯托克、珍珠港事件交织。
- **守笼人 / 阿妍 / 莹莹**：你的三位提示伙伴，各有讲解风格与权重（阿妍 0.6 / 守笼人 0.3 / 莹莹 0.1）。
- **伊藤**（特高课）等：对手阵营，构成 Boss 战与剧情张力。
- 叙事由**打字机引擎**驱动，含立绘、背景、音效；当前**配音（VO）暂缓**（见开发方向），仅保留打字机音效。

---

## 7. 特殊玩法模式（进阶）

| 模式字段 | 效果 |
|---|---|
| **错误限制**（limits.maxErrors） | 限定可错次数，超出判负，逼迫谨慎落子 |
| **静默电台**（silentMode） | 关闭或弱化剧情/语音干扰，纯解谜 |
| **密文抽取**（extract） | 关卡目标变为「抽提出隐藏密文」而非单纯填满 |
| **残卷改错**（plantedErrors） | 开局预填若干错误数字，需你发现并改正才能通关（找错改错玩法）|

---

## 8. 常见问题（FAQ）

**Q1：填错了怎么办？**
普通关：选中该格按 `Backspace` 擦除重填；错误数字会标红提示。限制错误数的关卡会更严格。

**Q2：卡住了？**
点「提示」走三级引导；或开「🧪 假设」模式试错；或看「热力图」找当前可推格。

**Q3：笔记怎么用？**
按 `N` 切到笔记模式，选中空格后按数字键记候选数（小字）。正式填数前清掉笔记再填。

**Q4：假设模式和普通填数有什么区别？**
假设模式在「分支快照」上操作，可随时回退（`Backspace`）或采纳（`A`/`Enter`），不影响主盘。适合验证大胆猜测。

**Q5：Boss 战输了？**
自动重玩该关，可调整节奏或先用假设模式推演关键据点。

**Q6：怎么看自己玩到哪一关了？**
按 `B` 打书签；关卡选择界面显示已解锁与进度。

**Q7：游戏需要联网吗？**
不需要。关卡、剧情、AI 推理全部本地运行，离线可玩。

---

> 本说明书为基于源码与既有分析报告的**玩家向**综述。更细的交互图文、求解器与架构细节，请参见仓库内 `cagemaster4-game-manual/` 与 `cagemaster4-dev-manual/`。
