# 音频制作决策锁定 — CageMaster4（换剧本后重适配）
> 锁定时间：2026-08-11 07:01 ｜ 依据：用户 5 项决策指令 + 审计报告
> 状态：**方向已定**，待补两项（weaver/W 冲突、第 6 点待补）后即可进入制作 brief。

---

## 一、VO 角色码映射（锁定 v1）
用户给定：`vera=V  sm=SM  suwan=W  ito=I  yamato=Y  plotter=P`

合并既有角色，完整新映射表（写入 `audio-service.js` 的 `CHARID_TO_VOCODE`）：

| 新 charId | 角色 | VO 码 | 状态 | 现有资产 |
|-----------|------|-------|------|----------|
| shenmo | 沈墨 | SM | ✅ 复用 | VO_SM 96 条 |
| vera | 薇拉 | **V** | 🆕 需新制 | 无（原 VO_J 莹莹 261 条转废弃，见冲突①） |
| suwan | 苏晚 | **W** | 🆕 需新制 | 无（原 W=星辰梭 被占用，见冲突②） |
| ito | 伊藤 | **I** | 🆕 需新制 | 无 |
| yamato | 山本 | **Y** | 🆕 需新制 | 无 |
| plotter | 设局人 | P | ✅ 复用 | VO_P 109 条 |
| cagekeeper | 守笼人/老师 | CK | ✅ 复用 | VO_CK 297 条 |
| plotterShadow | 设局人残影 | PS | ✅ 复用 | VO_PS 15 条 |
| remnant | 残局守护者 | RE | ✅ 复用 | VO_RE 8 条 |
| setterSecret | 设局人秘术 | SS | ✅ 复用 | VO_SS 34 条 |
| weaver | 星辰梭 | — | ❌ 已废弃移除 | 新剧本已无此角色（见冲突②） |
| narrator | 旁白 | N | ✅ 复用 | VO_N 51 条 |
| system | 系统 | S | ✅ 复用 | VO_S 5 条 |

> 修复 `_resolvePath`：去掉 `^([A-Z]+)_(\d+)` 硬正则，改为先查 `CHARID_TO_VOCODE`，再按 `VO_<码>_<####>.mp3` 解析目录。

### ⚠️ 两个冲突（已按用户指令解决）
- **① 原 VO_J（莹莹）261 条**：用户指令"原 vo 暂时不动"→ VO_J 资产**冻结归档**，不删不录；新剧本说话人列表已无"莹莹"、只有"薇拉(V)"，薇拉从零新制 VO_V_*。
- **② weaver 废弃，W=suwan 确认**：星辰梭(weaver) 已从新剧本移除，删除该角色行，`weaver` 行不再需要。完整新映射表见下表（已不含 weaver）。

---

## 二、音乐系统：多 Layer 情绪/节奏驱动（锁定）
用户指令：*"音乐要用多 layer 的情绪/节奏驱动音乐"* → **采用自适应分层音乐（adaptive layered music），非静态整曲 BGM。**

### 架构
- 每首"章节主题" = **多 stem 分轨**：`bed(铺底和声) / pulse(节奏脉冲) / motif(旋律动机) / tension(紧张层)` 四轨。
- 引擎按 **情绪状态 + 节奏强度** 实时调度：
  - 情绪状态（calm→tense→urgent→resolve）控制各 stem 音量/是否启用；
  - 节奏强度（0–1）控制 pulse/motif 的活跃度，借 `BeatQuantizer` 在拍点做 crossfade，避免相位爆音。
- 复用现有 `BGM_BPM_MAP` 作为 pulse 基准 BPM；新增 `emotionState` 输入（由 story-orchestrator 在场景切换/破局/危机时下发）。
- 这与审计报告 §三-3.2 的"三层 BGM"一致，但把"紧张层"从整曲升级为**可调度 stem**，更贴合你要的"情绪/节奏驱动"。

### 制作含义
- 作曲交付物 = **分轨 stem 包**（非单文件 mp3），每章主题 4 轨 × (intro+7章+outro+victory+fail)。
- 需新增轻量"分层音乐控制器"（Phase 2 实现，不改 SFX/VO 总线，风险可控）。

---

## 三、主题乐范围（锁定最小集）
用户指令：*"一共 7 章，至少需要 7 段新的主题乐 + intro，结尾可能还有 outro，一段 victory，一段 fail。"*

| 类型 | 数量 | 说明 |
|------|------|------|
| intro | 1 | 标题/序章，悬疑铺陈 |
| 章节主题 | **7** | 第 1–7 章各 1 段（情绪递进：克制→收紧→终局），每段出 4 stem |
| outro | 1（可选） | 真结局收束；若结局直接复用 ending_true 可省，建议保留 |
| victory | 1 | 通关 |
| fail | 1 | 失败/重试 |
| **合计（核心原创）** | **11 段 × 4 stem = 44 音频** | 这是作曲最低交付量 |

> 路线身份（沈墨/薇拉/伊藤区分）**不靠再写 21 段**，而靠"人物 motif 层"叠加在共享 7 章主题之上（见第四节）。若你实际想要每路线独立 7 段（共 21），请告知——那会是 84 stem 的量级。

---

## 四、人物主题乐（我拿主意 → 结论：做，但克制）
**结论：给 3 位 POV 主角各写一段短 motif（8–16 小节可循环），叠在章节主题之上作 route bed；反派共用 1 段"笼/设局"母动机。不做每个配角独立主题。**

- **沈墨(SM) motif**：小调下行三音 + 低音提琴拨奏 + 三角铁冷点（D 小调）——"我做过"。
- **薇拉(V) motif**：簧管慢琶音 + 远处钢琴回声（降六音的暖凉，bB 大调）——"我记得"。
- **伊藤(I) motif**：机械节拍(打字机/电报采样) + 弦乐长音 + 低通"档案室"感（g 小调）——"有证据证明发生过"。
- **笼/设局(共享反派) motif**：扭曲的笼式和声（45 法则动机），plotter/山本场景复用。
- 制作量：4 段 motif × 2–3 变体（安静/紧张）= 约 8–12 条短 stem，远小于章节主题。
- **理由**：3 视角共用谜题、分 POV 叙事是核心卖点，motif 是"听得出是谁的周目"的最便宜手段；全角色主题则超预算且无必要。

---

## 五、人物配音策略（逆转裁判参照，我拿主意 → 结论：混合式）
**结论：叙事节拍全量配音（学逆转裁判的"法庭戏"），关内玩法只配关键句。**

- **逆转裁判(Ace Attorney) 模式 = 全对话配音**，但那建立在其"短、高密度戏剧对白"上。本作 3 路线 × 81 关 = 243 关，若全量配音是数千条录音，成本/周期不可行。
- **推荐混合式**：
  - ✅ **全量 VO**：所有叙事对话——关前序章(pre) / 通关(clear) / 章节序章(prologue) / 隐藏关 / 各路线结局。这是"逆转裁判感"的来源，戏剧张力最强处。
  - ✅ **关键句 VO**：关内教学/玩法对白只配**关键节点句**（破局点、反转、情绪高点），其余走文字+打字机。
  - ❌ 不做：每关每句全量（超预算）、也不做纯静音（丢沉浸）。
- **制作管线**（延续审计报告）：先跑"数字→汉字 / ×→乘 / =→是"清洗脚本，再录制；逐行挂 `voiceId`；最后翻 `VOICE_ENABLED=true`。
- **新角色 VO 制作量（混合式下）**：SM 补录缺口 + V/I/W/Y 从零（叙事全量 + 关键句），预计 V/I 各 80–120 条、W/Y 各 40–60 条、SM 补 30–50 条。

---

## 六、下一步（待你回两项冲突后）
1. 确认 ①/②（莹莹资产废弃？星辰梭改码 WR 或移除？）→ 我锁定完整 `CHARID_TO_VOCODE` 并改 `_resolvePath`。
2. 据此产出 **作曲 brief 包**：11 段主题乐 × 4 stem 的分轨规格 + 4 段人物 motif 规格（含调式/BPM/乐器/情绪标注），可直接喂 AI 音乐生成或真人作曲。
3. 阶段0 接线修复（CHARID 映射 + `_resolvePath`）+ 阶段1 补 chapter_8/9 与 `playRoute` 原型，先让 BGM/VO 不再静默。

> 第 6 点你的消息在此截断（仅"6"），请补全后我一并处理。

---

## 七、资产隔离 + 工具链（无预算，用户续拍板）
用户续指令：① **原 VO 暂时不动**（旧 12 码 assets/audio/voice/ 冻结）；② **新开文件夹**存新 audio；③ **weaver 废弃，W=suwan 确认**（无需 WR 码，删 weaver 行）；④ 第 6 点=问"用哪个 AI 工具？我们没预算"。

### 7.1 新资产目录（与旧隔离）
```
assets/audio_next/            ← 新音频根（旧 assets/audio/ 冻结不动）
├── bgm/                     ← 11 段主题乐 × 4 stem（intro+7章+outro+victory+fail）
├── sfx/                     ← 6 类时代音(coin/telegraph/radio/cabinet/pen/siren) + NEO 替换
└── voice/                   ← 仅新码：V(薇拉)/I(伊藤)/W(苏晚)/Y(山本)
```
- 引擎改造：`audio-service.js` 增加 `AUDIO_ROOT_NEXT` 开关；加载时**先查 audio_next，未命中回退 assets/audio**（旧 SM/P/CK/… 照常可播，新 V/I/W/Y 只在新目录）。原 VO/Voice_index 不改动。
- 命名沿用 spec §2：`bgm/chapter_3_pulse.mp3`、`voice/V/VO_V_0001.mp3` 等。

### 7.2 工具栈（按用户现有所更新 — 本地、0 预算、可商用）
用户已备：**IndexTTS2（本地 VO）** + **ACE-Step 1.5（本地音乐）**。据此更新：

| 用途 | 工具 | 说明 |
|------|------|------|
| **主题乐 / 分层 stem** | **MiniMax**（主，用户确认仍保有额度） | 与现有 16 首 BGM 同厂牌→音色统一；按 4 stem 分 prompt 各生成一段（"solo cello pad, no percussion" / "taiko pulse only" / "melody motif" / "tense strings swell"），引擎叠层实现情绪/节奏驱动 |
| 主题乐(备用) | **ACE-Step 1.5**（本地） | 若 MiniMax 额度告急/离线则退回本地 ACE-Step，prompt 严格对齐 noir 模板以贴齐既有听感 |
| **人物 VO(中文+情绪)** | **IndexTTS2**（本地） | 支持情绪标签 + 声线克隆；为 V/I/W/Y(+补录 SM) 各指定稳定声线（克隆参考音或固定种子），录制前强制跑"数字→汉字"清洗 |
| **SFX 设计音** | 见第八节（程序化合成为主 + Freesound CC0 + ACE-Step 补环境） | — |
| 必要时的分轨 | **Demucs**(MIT) | 仅当用整曲再拆鼓/贝斯/人声时备用 |

- **为何不推荐 SUNO/Udio/Edge TTS**：SUNO/Udio 免费额度有限且商用授权模糊；Edge TTS 微软 ToS 限制商用再分发。现有 IndexTTS2 + ACE-Step 1.5 均为本地开源/可商用，正好覆盖 VO+音乐，0 API 成本。
- **ACE-Step 分层做法**：AI 音乐出的是混音立体声，拿不到语义 stem。实操=**每个 stem 单独写 prompt 生成**（bed/pulse/motif/tension 各一条），引擎按情绪状态+节奏强度实时调各 stem 音量/启停。这与"多 layer 情绪/节奏驱动"完全对齐。
- **环境**：均 Python 本地跑，建议 NVIDIA GPU；无 GPU 可小模型 CPU 慢跑 / 免费 Colab。我可帮你**搭生成管线脚本**（按本 spec 的 stem 规格批量出提示词→调模型→落盘 audio_next），你本地一键跑。
- **质检**：生成后按 spec 听审（loop 无缝、调式/BPM 对、VO 数字已转汉字），再落盘命名。

---

## 八、SFX 音效方案（用户要求补齐）
**原则**：现有 `assets/audio/sfx/`(94 文件) 大部分可复用（paper/door/footstep/typewriter/combo/emotion/rating），**不重做**；只补"间谍时代缺口 + 多样本变体 + NEO 替换"。SFX 以**程序化合成**为骨干（机械/木质音合成比 AI 更干净可控、且天然支持无限多样本），环境纹理用 **Freesound CC0** 或 **ACE-Step** 补。

### 8.1 复用（不动）
核心玩法(click/fill_*/erase/note_toggle/hint)、UI(paper_flip/book_*/seal_*)、连击(combo_*)、评级(rating_*)、情绪(emotion_*)、环境(ambient_wind/thunder) → 直接沿用旧 sfx/。

### 8.2 新增（落 audio_next/sfx/）
| 事件 | 生成法 | 触发场景 | 做法要点 |
|------|--------|----------|----------|
| coin_drop | 程序化(FM金属) | 薇拉窗台留硬币信号 | 两正弦泛音+快衰减+微噪声瞬态,250ms,**3 变体** |
| telegraph_key | 程序化(带通噪声) | 伊藤档案室/发报 | 共振带通~2kHz 噪声爆+机械咔嗒,80ms,**3 变体** |
| radio_static | Freesound CC0 / ACE-Step | 无线电余韵 | 滤波白噪+偶发噼啪,loopable 1s |
| file_cabinet | 程序化(低通噪声涌) | 档案柜抽拉 | 木质共振+噪声涌,400ms,**3 变体** |
| pen_ink | 程序化(滤波噪声+LFO) | 钢笔吸墨 | 低频气泡涌动,200ms |
| distant_siren | 程序化(正弦扫频+低通) | 孤岛夜警 | 重低通+慢颤音,1.2s,loopable |

### 8.3 多样本变体（spec §8 缺口①）
脚步(footstep/wood/stone/hall×2/run)、打字机(typewriter/key)、门(door_open/light/stone/final) 当前多为单样本 → **程序化重生成各 3 变体**（随机种子参数即无限干净变体），覆盖旧 sfx/ 同名文件或存 NEO/。

### 8.4 NEO 替换（spec §8 缺口③）
`door_open / fill_correct` 等优先用 NEO/ 高质量变体替换根目录低质样本（程序化或 ACE-Step 生成后对比选取）。

### 8.5 程序化合成脚本
我可写 `scripts/gen-sfx.py`（numpy/scipy）：定义每个音的**参数化合成函数**（coin/telegraph/cabinet/pen/siren/footstep/typewriter/door），循环生成 N 变体 → 落 `audio_next/sfx/`。这样多样本/调参零成本，且比 AI 更贴合"木/纸/石/机械"有机质感（审计报告声音方向要求）。

> SFX 结论：**复用旧 94 + 程序化补 6 类时代音 + 程序化补多样本变体 + NEO 替换**，0 预算、质感可控、天然多样本。

---

## 九、生成脚本工具链（已搭建 ✅）
1. `assets/audio_next/` 骨架已建：`bgm/`、`sfx/`、`voice/{V,I,W,Y}/`（旧 `assets/audio/voice/` 12 码冻结不动）。
2. 三个生成脚本（已落地 `scripts/audio/`，均通过冒烟测试）：
   - **`gen-sfx.py`**（numpy 程序化）：实测跑通，**生成 37 个有效 WAV** 落 `audio_next/sfx/`（6 类时代音 coin/telegraph/radio/cabinet/pen/siren + 多样本 footstep×4 材质 / typewriter / door×4 各 3 变体）。修复点：①奇数 n 致 irfft 长度与包络不一致→强制偶长；②MULTI_SFX 的 name 未带 kind→跨材质同名覆盖→改为含 kind 文件名。
   - **`gen-music.py`**（MiniMax 主 / ACE-Step 备）：产出 `audio_next/bgm/prompt_manifest.json`（**52 条**：11 主题×4 stem + 4 人物 motif×2 变体），含 BPM/情绪/乐器/4-stem 差异化提示词；`--live` 接 MiniMax 异步音乐 API 真实生成（需 `MINIMAX_API_KEY`/`MINIMAX_GROUP_ID`），失败单条不阻塞。
   - **`gen-vo.py`**（IndexTTS2 本地）：内置 `clean_vo_text()` 强制"数字→汉字 / ×→乘 / =→是"清洗（已 demo 验证）；按 `VOICE_PROFILES`(V/I/W/Y/SM) + `--manifest vo_lines.json` 批量出 `audio_next/voice/<CODE>/VO_<CODE>_####.wav`；`--live` 接本地 IndexTTS2。
3. **你本地 GPU 一键跑**：
   - `python scripts/audio/gen-sfx.py`（无 GPU 也能跑，纯 numpy）
   - `python scripts/audio/gen-music.py` 先核对手感（仅写 manifest），再 `--live` 真生成
   - `python scripts/audio/gen-vo.py --demo` 看清洗 → 备好 `vo_lines.json` 后 `--live` 出 VO
4. 听审 → 按命名规范归位 → **阶段0+1 接线修复让 BGM/VO 不再静默**（见下条待办）。

> 注：VO 实际录制前，需先从 `scripts.json`/关卡 JSON 抽取 VO 台词清单写成 `vo_lines.json`（字段：`{code,id,text,scope?,emotion?}`）；此抽取器可另立小脚本，本期未做。

---

## 十、既有 BGM 来源与风格连续性（用户补充：原来用 MiniMax 做的）
用户一句："原来我们 minimax 做的音乐" → **现有 `assets/audio/bgm/` 16 首（intro/chapter_1..7/boss/eureka/hidden/victory/ending_×4）均为 MiniMax 生成**。

### 10.1 含义
- **来源清晰、无第三方授权意外**：现有 BGM 是 AI 生成（MiniMax），非外购曲库，重适配时不必担心外部版权。
- **质量/风格基线已知**：noir 1941 上海方向已被 MiniMax 落地过，新音乐应**延续该音色与配器**（钢琴+弦乐+簧管+木/纸/石质感），否则旧新段落会"听出两个作者"。

### 10.2 风格连续性风险（核心）
新音乐计划用 **ACE-Step 1.5（本地）**，旧音乐是 **MiniMax** —— 两套模型音色/混音习惯不同，直接混用会出现**可闻的"换作曲家"接缝**。需在动工前定方向：

| 方案 | 做法 | 一致性 | 成本 |
|------|------|--------|------|
| **A. 仍用 MiniMax 做新音乐** | 新 intro/7章主题/outro/victory/fail + 路线 motif 全交 MiniMax | ✅ 完全统一 | 需 MiniMax 额度（你之前说"没预算"，故待确认是否还有可用额度） |
| **B. 全本地 ACE-Step 统一重渲** | 旧的 7 章也用 ACE-Step 按同一 noir 模板重渲，彻底统一 | ✅ 完全统一 | 工作量翻倍（重做 7 首旧的） |
| **C. 混合（默认推荐）** | 旧 MiniMax 7 章作 1–7 章紧张层保留；新 8/9 章 + intro/outro/victory/fail + 路线 motif 用 ACE-Step，prompt 严格对齐 noir 模板 | ⚠️ 接缝在章边界（比关内接缝轻） | 最低 |

### 10.3 待确认
- **是否仍保有 MiniMax 访问/额度？** 有→方案 A（最省心一致）；无→方案 B 或 C。
- 选 C 时，建议在 ACE-Step prompt 里固定"乐器清单 + 混响量 + BPM 区间"三段式，最大限度贴齐 MiniMax 既有的 noir 听感。

> **✅ 用户确认"有"MiniMax 额度 → 采用【方案 A】**：新音乐(intro/7章主题/outro/victory/fail + 路线 motif）全部用 MiniMax 生成，与现有 16 首音色完全统一；ACE-Step 1.5 仅作离线/额度告急备用。VO(IndexTTS2) 与 SFX(程序化) 不变。音乐工具行已锁定。

> 注：此条与上条"无预算"需一起看——若 MiniMax 已无额度，则新音乐走本地 ACE-Step（方案 C 最务实）。VO 与 SFX 不受影响（IndexTTS2 / 程序化 不变）。
