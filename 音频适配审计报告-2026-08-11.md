# 音频适配审计报告 — CageMaster4（换剧本后重新适配）
> 审计时间：2026-08-11 ｜ 范围：音效(SFX) + 音乐(BGM) + 配音(VO) 全链路
> 对象：`audio/audio-service.js`(1706行) · `docs/audio-production-spec.md`(Phase1) · `assets/audio/`(1316 mp3 + 34 wav) · `story/story-engine.js` · `story/story-orchestrator.js` · `data/scripts/scripts.json` · `莹莹配音台词问题分析报告.md`
> 结论：**引擎本身健壮（Web Audio 双总线、ducking、交叉淡入、合成兜底、优雅降级都到位），但"换剧本"导致两层断裂亟待修复——① 角色 ID 两套体系互不连通（故事引擎新 charId vs 音频旧 VO 码），VO 一旦开启会整体静默/悬空；② BGM 仍按 chapter_1..7 单维命名，而新结构是"三路线 × 多章"，缺 chapter_8/9 文件且无路线音乐身份。音乐制作方向需从"章节 BGM"升级为"路线 motif + 章节层 + 事件 sting"三层架构。**

---

## 一、现状快照（审计基线）

### 1.1 音频引擎（audio-service.js）能力盘点 — 良好，无需大改
- **双总线**：master/sfx/voice/bgm 四增益 + bgm 低通滤波（`setLowPass`/`transition`）。
- **VO 播放**：`voice.play(voiceId)` → `_resolvePath` 解析 `VO_<CODE>_<####>.mp3` → 查 `VOICE_CHAR_MAP` → 加载；含 ducking（语音时 SFX−6dB/BGM−3dB）、淡入淡出、时长索引（voice_index.json）。
- **BGM 播放**：`bgm.play(chapterId)` → `chapter_<id>.mp3`；`playFile(name)`；`playBoss`；交叉淡入 800ms；`BGM_BPM_MAP` 供节拍对齐。
- **合成兜底**：`_playTypewriterKey`(噪声+三角波)、`synth.playAvalancheStart/Tick/End`、`playActOpen/Breakthrough` —— 即使缺资源也不崩。
- **优雅降级**：SFX/VO 加载失败 `console.debug` 静默跳过，不阻断剧情。
- **音量**：master/sfx/voice/bgm 四档持久化（localStorage），默认值 `bgm:0.4` 偏低。

### 1.2 资产盘点（assets/audio/）
| 类别 | 文件数 | 说明 |
|------|--------|------|
| bgm/ | 16 (14 mp3 + 2 wav) | intro / chapter_1..7 / boss_battle / eureka / hidden_level / victory / ending_circle1..3 / ending_true |
| sfx/ | 94 (62 mp3 + 32 wav) | 核心玩法/UI/环境/连击/评级/情绪，含 NEO/ 高质量变体 |
| voice/ | 1226 条 mp3 + voice_index.json | 12 旧角色码：CK297/N51/SM96/J261/R336/PS15/RE8/W13/SS34/P109/S5/U1 |

### 1.3 接线盘点（谁触发音频）
- **故事层** `story-orchestrator.js`：
  - L236-241 进关 BGM：`bgm = 'chapter_' + chapterId + '.mp3'`（**仅按 chapterId，无 cycle 维度**）；
  - L305 序章 `intro.mp3`；L385 隐藏关；L241 `bgm.playFile(bgm)`。
- **对话层** `story-engine.js`：
  - L1336/L1420 `hasVoice = line.voiceId && VOICE_ENABLED && AudioService` → `AudioService.voice.play(line.voiceId)`；
  - `VOICE_ENABLED = false`（L16），**VO 整体暂缓**。
- **对话数据** `data/scripts/scripts.json`：383 行带 speaker，**当前 0 个 voiceId 字段**（dialog 不挂语音）；部分 level JSON（level-199/201/202…）带 voiceId。说明语音是"按关卡/逐行挂接"模式，当前未大规模铺设。

---

## 二、核心发现（按严重度）

### 【P0-高】1. 角色 ID 两套体系断裂 → VO 全量静默/悬空
【位置】`story-engine.js:553` `SPEAKER_ALIASES`(新) vs `audio-service.js:41` `VOICE_CHAR_MAP`(旧)
【事实】新剧本角色映射（新 charId）：
`shenmo(沈墨) / suwan(苏晚) / vera(薇拉) / cagekeeper(守笼人=老师) / plotter(设局人) / plotterShadow(残影) / weaver(星辰梭) / remnant(残局守护者) / setterSecret(设局人秘术) / narrator(旁白) / system(系统)`
旧音频 VO 码：`SM / J / N / P / PS / R / RE / S / SS / U / W / CK`
【断裂点】`voice._resolvePath`(audio-service.js:515) 用正则 `^([A-Z]+)_(\d+)` 解析 voiceId，并查 `VOICE_CHAR_MAP` 校验。新 charId 是**小写多字母**（shenmo/suwan/vera…），既不匹配正则，也不在旧 MAP 中 → 解析返回 null → **优雅跳过（静默）**。即使 `VOICE_ENABLED=true`，所有新角色语音都播不出来。
【修复方向】在 audio 层新增 `CHARID_TO_VOCODE` 翻译表，把新 charId 映射到 VO 文件码，并修正 `_resolvePath` 兼容小写/多字母：
```
shenmo→SM, suwan→SW(新), vera→J(若复用莹莹)或V(新), cagekeeper→CK,
plotter→P, plotterShadow→PS, weaver→W, remnant→RE, setterSecret→SS,
narrator→N, system→S
```

### 【P0-高】2. 新角色 VO 资产缺失（苏晚=0；薇拉疑似复用 J 待确认）
【事实】voice_index.json 现有 1226 条，覆盖旧 12 码。**新剧本核心角色缺位**：
- **苏晚(suwan)**：0 条，全新角色，需从零制作全套 VO。
- **薇拉(vera)**：新剧本 cycle2 POV。现有 `VO_J_*`(莹莹) 261 条。两者是否为**同一角色更名**（温暖镜像系，可能性高）还是独立新角色，**需你确认**。若是更名，则 261 条可直接复用（仅需改 charId 映射 + 文本清洗，见 §三-4）；若是新角色，则也需从零制作。
- **沈墨(shenmo)**：`VO_SM_*` 96 条已存在，可复用（数量偏少，81 关 pre/clear 对话可能需补录）。
- 其余旧码（CK/N/P/PS/R/RE/W/SS/S/U）基本覆盖守笼人/旁白/设局人等，可复用。
【修复方向】先确认 vera↔J 关系；制定 suwan 全量 VO 制作计划；补录 SM 缺口。

### 【P0-高】3. BGM 章节制 vs 三路线结构：缺 chapter_8/9 + 无路线音乐身份
【位置】`story-orchestrator.js:238` `bgm='chapter_'+chapterId+'.mp3'`；`assets/audio/bgm/` 仅 chapter_1..7
【事实与风险】
- **文件缺口**：现有 BGM 仅 chapter_1..7。新结构含多路线、章节号可能超 7（如 8xx Boss 章、9xx 薇拉章）→ `chapter_8.mp3`/`chapter_9.mp3` **不存在** → 这些章 BGM 静默（fetch 404，引擎静默跳过）。
- **无路线身份（更严重）**：三 POV（沈墨/薇拉/伊藤）共用 chapter_1..7 同名文件，**音乐上完全无法区分三条叙事线**。对"三岔口"三视角共用谜题、分 POV 叙事的核心卖点而言，这是体验级缺陷——玩家听不出"现在是谁的周目"。
【修复方向】见 §三 音乐制作方向（路线 motif 层 + 章节层双层架构）。

### 【P1-中】4. VO 文本预处理未落地（数字/符号/标点）
【事实】`莹莹配音台词问题分析报告.md` 已指出 `VO_J_*` 约 40 条需清洗：阿拉伯数字→汉字（"112"→"一百一十二"、"2×2"→"二乘二"、"="→"就是"）。若 vera 复用 J，这些清洗仍需执行；若新制 suwan/vera/route VO，必须**在制作管线强制数字转汉字规则**，否则配音演员会念错。
【修复方向】建立 VO 文本清洗脚本（数字→汉字、×→乘、=→是/就是、嵌入引号转义），在录制前跑一遍；纳入 CI。

### 【P1-中】5. 配音总开关 VOICE_ENABLED=false（VO 整体暂缓）
【事实】`story-engine.js:16` 默认 `false`；`下一阶段开发方向.md` 也将其列为"整体暂缓"。
【影响】当前所有 VO 资产（1226 条）均未接入播放。重启用前必须先解决 #1（ID 映射）、#2（资产缺口）、#4（文本清洗）。
【修复方向】#1/#2/#4 完成后，翻 `VOICE_ENABLED=true` 并做一轮全量回放验证。

### 【P2-低】6. SFX 大体可复用，但间谍时代需补时代音 + NEO 替换
【事实】现有 SFX（paper/door/footstep/typewriter/combo/emotion/rating）主题中性，换剧本**基本可直接复用**。但新基调"1941 上海谍战"可增补：
- **时代科技音**：无线电余韵/电报键(key)/发报机脉冲（现仅 `electronic_pulse` 偏现代）、打字机已覆盖；
- **剧情专属**：硬币落地（薇拉留给伊藤的"已离沪"信号）、木抽屉/档案柜、钢笔吸墨、远处警笛；
- **NEO 替换**：spec §8 指出 `door_open/fill_correct` 等应优先用 NEO/ 高质量变体，尚未全面替换。
【修复方向】列"新增 SFX 清单"，优先补硬币/电报/档案柜三类剧情关键音。

### 【P2-低】7. 结局音乐映射待核对
【事实】spec 有 ending_circle1..3 + ending_true（圆环结局×3 + 真结局）。新剧本三路线各有终局弧（沈墨"离开"/薇拉"短横"/伊藤"残る"），需确认这 4 首是否足够覆盖三路线结局，或需按路线拆分。
【修复方向】剧情定稿后核对每条路线结局用哪首，必要时扩 ending 系列。

---

## 三、音乐制作方向（核心交付）

### 3.1 总声音方向（重申并收紧）
- **基调**：1941 上海孤岛 noir + 笼式数独；阴冷克制、机械精密、暗流涌动。
- **时代感**：器乐走"老上海 × 悬疑电影配乐"——钢琴、弦乐(低音提琴/中提)、簧管(单簧/双簧)、钟琴、木/纸/石/机械打键质感；**禁用现代 EDM/合成主音色**（stinger 例外）。
- **人声**：BGM 纯器乐，无人声；VO 为唯一人声通道。
- **技术约束**：单条 SFX ≤2s 解码驻留；BGM/环境流式；统一 192k mp3（BGM）、96–128k mp3（VO）、PCM wav（短 SFX）。

### 3.2 三层 BGM 架构（解决 #3 路线无身份 + 缺文件）
把现有"单维 chapter"升级为 **路线 motif 层 + 章节紧张层 + 事件 sting**：
1. **路线 motif 层（新增，核心）**：为三条 POV 各写一段可被辨识的**主导动机(leitmotif)**，作为底层和声/音色标识，贯穿该路线所有 BGM：
   - **沈墨(shenmo)**：冷峻行动系。动机=小调下行三音 + 低音提琴拨奏 + 三角铁冷点；调式偏 D 小调；节奏方整、推进感。关键词：**"我做过"**（行动叙事）。
   - **薇拉(vera)**：记忆/镜像系。动机=慢速琶音 + 簧管(单簧管) + 远处钢琴回声；调式偏 bB 大调但带降六音（回忆的暖与凉）；节奏自由、留白多。关键词：**"我记得"**（记忆叙事）。
   - **伊藤(ito)**：档案/官僚系。动机=机械节拍(打字机/电报采样) + 弦乐长音 + 低通滤波"档案室"感；调式偏 g 小调；节奏冷静、像在"阅读"。关键词：**"有证据证明发生过"**（存在叙事）。
   - 每路线 motif 写成 4–8 小节可循环短句，作为"路线床(route bed)"常驻低音量。
2. **章节紧张层（复用现有）**：chapter_1..7（及补齐 8/9）作为"紧张度层"，在 route bed 之上叠加——保留现有 7 首的 BPM 递增(96→124)与情绪递进，但**每首重新混音以嵌入对应路线 motif**。
3. **事件 sting（新增，一次性）**：eureka/breakthrough/victory/ending/隐藏关 维持现有，但按路线给变体（如 eureka_shenmo 更锐利、eureka_vera 更柔光）。

> **资产落地**：现有 chapter_1..7 不废，改为"通用紧张层模板"，新做 3 条 route bed（每条约 60–90s 循环）+ 把 7 首重混进路线。补齐 chapter_8/9（Boss 章/薇拉 9 章）各一套紧张层，同样叠路线 motif。

### 3.3 路线 × 章节 触发改造（解决 #3 静默）
`story-orchestrator.js:238` 改为按 **route+chapter** 取 BGM：
```
bgm = `route_${cycleId}` + (routeBed) 常驻；
紧张层 = `chapter_${chapterId}.mp3`（补齐 8/9）；
// 进入路线时先播 route bed，章节切换时交叉叠紧张层
```
并扩展 `BGM_BPM_MAP` 与 `audioService.bgm.playRoute(cycleId)`。这样三条周目各听各的 motif，章节紧张度照常递进。

### 3.4 VO 制作管线（解决 #1/#2/#4/#5）
1. **建翻译表** `CHARID_TO_VOCODE`（见 #1），修 `_resolvePath` 支持小写多字母 charId。
2. **确认 vera↔J**：若复用，把现有 `VO_J_*` 视为 vera 资产（目录可改名 `VO_V_` 或保留 J 并在映射里 `vera→J`）；若独立新角色，新制 `VO_V_*`。
3. **新制 suwan(VO_SW_*)**：按角色人格（旧友、与"你老师"相关、温润带悬念）写台词并录制，数量参照其他主角（建议 ≥80 条覆盖其出场章）。
4. **文本清洗强制化**：录制前跑清洗脚本（数字→汉字、×→乘、=→是/就是、引号转义），复用莹莹报告规则。
5. **逐行挂接 voiceId**：在 scripts.json / level JSON 的 dialog 行补 `voiceId` 字段（当前 0 个），先做关键叙事行，逐步铺满。
6. **翻 VOICE_ENABLED=true**，全量回放校验。

### 3.5 新增 SFX 清单（解决 #6，按剧情重要性）
| 事件 | 建议文件 | 触发场景 | 生成提示 |
|------|----------|----------|----------|
| coin_drop | coin_drop.wav | 薇拉窗台留硬币信号 | soft metal coin on wood, 250ms |
| telegraph_key | telegraph_key.wav | 伊藤档案室/发报 | morse key click, dry, 80ms |
| radio_static | radio_static.mp3 | 无线电余韵 | distant radio hiss loop, 1s |
| file_cabinet | file_cabinet.wav | 档案柜抽拉 | wooden drawer slide, 400ms |
| pen_ink | pen_ink.wav | 钢笔吸墨 | fountain pen fill, 200ms |
| distant_siren | distant_siren.mp3 | 孤岛夜警 | far police siren, lowpass, 1.2s |

### 3.6 技术规格（沿用 spec，补两点）
- **循环无缝**：route bed 与 chapter 层必须 seamless loop（首尾 50ms 交叉淡），避免每轮循环"咔哒"。
- **分层混音预算**：route bed(−18dB) < chapter 层(−12dB) < sting(0dB)；VO 永远在顶（ducking 已支持）。建议把默认 `bgm:0.4` 提到 `0.5` 让路线 motif 可闻。

---

## 四、落地路线图（分阶段，最小改动先跑通）

| 阶段 | 目标 | 交付 | 风险 |
|------|------|------|------|
| **阶段0·接线修复(P0)** | VO 能播 | 加 `CHARID_TO_VOCODE` + 修 `_resolvePath`；确认 vera↔J | 低（不改播放逻辑） |
| **阶段1·BGM 不静默(P0)** | 所有章有 BGM | 补齐 chapter_8/9 紧张层（可先复制 7 改 BPM）；`bgm.playRoute` 原型 | 低 |
| **阶段2·路线身份(P0精神)** | 三路线听得出 | 新做 3 条 route bed + 重混 7 首叠 motif；改造触发逻辑 | 中（创作+混音工作量） |
| **阶段3·VO 资产(P0/P1)** | 新角色有声 | suwan 全量 VO；vera 清洗复用；SM 补录；文本清洗脚本；逐行挂 voiceId | 中（制作量） |
| **阶段4·SFX/结局(P2)** | 时代感补全 | 新增 6 类时代音；NEO 替换；结局映射核对 | 低 |
| **阶段5·启用** | 翻 VOICE_ENABLED=true | 全量回放验证 + 修 §二 残余 | 低 |

**建议起点**：先做阶段0+阶段1（纯接线与补文件，不改播放内核），可立刻消除"换剧本后 BGM/VO 静默"的硬伤，且风险最低、收益最显。路线 motif（阶段2）是"音乐制作方向"的重点艺术投入，可在阶段0/1 跑通后单独排期。

---

## 五、待你确认的两个决策（影响工作量巨大）
1. **薇拉(vera) 是否 = 莹莹(J) 更名复用？** 若是 → 261 条 VO 直接复用（仅映射+清洗）；若否 → 需新制 VO_V_*（同 suwan 工作量）。
2. **伊藤(ito) 是否有专属 VO 码？** 现有 voice_index 无 ito 码；伊藤是 cycle3 POV，若要有声，需新增 VO_IT_* 全套（类似 suwan）。请确认伊藤是否也需配音，还是保留"档案/留声"式非人声处理。

*Audit v1.0 — 2026-08-11. 覆盖 SFX/BGM/VO 全链路，聚焦"换剧本"引入的断裂。*
