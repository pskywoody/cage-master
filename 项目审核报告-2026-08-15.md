# 《笼中密信：上海1941》（CageMaster4）项目审核报告

> 审核日期：2026-08-15
> 本次更新：**核心引擎专项静态复核**（聚焦求解 / 教学 / Boss / 关卡工程，只读取证、未运行脚本）
> 审核方式：只读遍历源码、关卡数据、配置、测试、报告与文档，未修改任何源码/数据（仅更新本报告文本）
> 仓库：`D:\killersudoku\cagemaster4`
> 当前代码版本：约 v4.3.x（game.html / ui/main.js 标注 V4.3.31–V4.3.33）

---

## 一、项目概况

《笼中密信：上海1941》（Caged Cipher: Shanghai 1941）是一款以「杀手数独（Killer Sudoku）」为核心玩法、叠加 1941 年上海谍战叙事与 Boss 对决的网页解谜游戏。

技术形态是纯前端应用：HTML5 + Canvas 2D + 原生 ES Modules JavaScript，无构建打包流程、无后端依赖，关卡与剧情数据全部本地加载，离线可玩。进度与设置通过 localStorage 持久化。

规模方面，关卡共 **7 章 × 9 关 = 63 关**（编号 101–709），支持 4×4、6×6、9×9 三种盘面；静态计数为 4×4 8 关（101–108）、6×6 5 关（109/201/202/203/209）、9×9 50 关。主入口 `game.html` 约 40 万字符、8000 余行，是典型的「单文件大前端 + 模块化外部 JS」结构。全仓约 105 个 JS 文件，`node --check` 语法检查全部通过（本次静态复核未重跑）。

四个入口页面各司其职：`game.html` 是主游戏；`index.html` 是 Canvas 渲染里程碑演示；`replay.html` 是对战回放；`ai-debug.html` 是 AI 调试面板。

---

## 二、目录结构与职责

| 目录 | 职责 |
|---|---|
| `core/` | 数独引擎、技巧求解（TechRater）、关卡管理、Boss 战（含三点连线）、AI 人格、戏剧导演、推理监控、调查系统 |
| `renderer/` | 棋盘渲染、动画控制、特效、提示动画适配、战场可视化、性能监控 |
| `ui/` | 主入口 `main.js`、设置/关卡选择/图鉴/调查/成就/连击/自由模式等面板、书本式外壳 |
| `story/` | 剧情引擎（打字机）与剧情编排器 |
| `content/` | Boss 战内容包（台词 / 阶段 / 反馈，中英日三语） |
| `data/` | 63 关关卡数据、章节数据、验收与回归结果 JSON |
| `i18n/` | 四语文案、locale 资源、术语对照表、对齐审计 |
| `config/` | production / research 配置 |
| `scripts/` | 关卡校验、AI 测试、数据迁移、回归脚本 |
| `tests/` | 笼子生成器测试 |
| `docs/` `reports/` | 设计文档、验收报告、实验报告、交接评估 |
| `assets/` `audio/` | 立绘/CG/音频资源与音频服务 |
| `cagemaster4-*-manual/`、`release-readiness-assessment/` | 开发手册、玩家手册、上线就绪评估（HTML） |

一个需要指出的问题：`backend-engine/` 目录虽然位于本仓库内，但其中的 `rule_coverage_matrix.md`、`engine_cases.json` 实为**另一个项目「BestBay」英语学习决策引擎**的覆盖审计（D/S/C/P 四族 17 条规则），与杀手数独技巧无关。它不应被当作本项目的测试资产。

---

## 三、核心玩法与求解引擎

### 3.1 规则模型

引擎在 `core/board.js` 与 `core/headless-engine.js` 中实现。每个格子（Cell）包含固定数 `fixedNum`、填入数 `fillNum`、候选数 `candidates`、排除集 `eliminations`、错误标记 `isError`、笼和值超限 `isCageSumError`、教学锁定 `isLocked` 等字段。误填标红有双来源：全盘重复扫描（行/列/宫/笼，`Board.checkConflicts`）加上「与标准解比对」（在 `headless-engine.js` 层），任一不满足即标红。

三套和值法则统一为一条参数公式：`core/tech-rater.js` 的 `getRule45Sum(size)` 返回 `size*(size+1)/2`，4×4 / 6×6 / 9×9 分别得 10 / 21 / 45（即 10 法则 / 21 法则 / 45 星衡法则），全库没有「10法则/21法则」的独立字面实现。星衡法则的内突（Innie）/ 外突（Outie）推导在 `_rule45ForScope`（`tech-rater.js` L1323–1452）实现，并生成可展示的 `formula` 证据；适用于 6×6 / 9×9（4×4 优先级未启用）。

### 3.2 技巧阶梯

求解引擎 `core/tech-rater.js` 定义了一套「取最低可用技巧」的人类化阶梯（`TECHNIQUES`），共 **10 个核心技巧**，按三个优先级数组启用：

| 级别 | 代号 | 中文名 | 适用尺寸 |
|---|---|---|---|
| 1 | nakedSingle | 孤星 | 4/6/9 |
| 2 | cageUnique | 唯一组合 | 6/9 |
| 3 | hiddenSingle | 隐曜 | 4/6/9 |
| 4 | rule45 | 星衡法则 | 6/9 |
| 5 | nakedPair | 并蒂锁 | 4/6/9 |
| 6 | hiddenPair | 双曜 | 6/9 |
| 7 | pointingClaiming | 区块排除 | 6/9 |
| 8 | nakedTriplet | 三子法 | 6/9 |
| 9 | xWing | 二连纵横阵 | 9 |
| 10 | swordfish | 三才游鱼阵 | 9 |

优先级数组经查证为：9×9 含上述 10 项（`TECH_PRIORITY_9`）、6×6 含前 8 项到 nakedTriplet（`TECH_PRIORITY_6`）、4×4 仅 nakedSingle / hiddenSingle / nakedPair 三项（`TECH_PRIORITY_4`）。因此 **4×4 不含 cageUnique / rule45，无法使用笼和推理**——与 board.js 已支持 `cageIds` 多笼存在能力错位。

此外定义表中第 11 项 `guess`（试数，level 11 / depth 6）声明于 `TECHNIQUES`，但三个优先级数组均未包含它，且 `_findAllByTechnique` 的 switch 无 guess 分支、default 返回空，故**试数声明但确实未启用**，引擎坚持「不做猜测、只走逻辑推理」。

### 3.3 三级提示与证据链

三级渐进提示在 `core/board.js` 的 `showHint` 中落地：第一级标注目标格，第二级高亮区域/数对，第三级给出具体答案数字；下一最优步骤由 TechRater 产出，并携带 `evidence`（如星衡法则的公式、被排除的候选）。`ui/ledger-hint.js` 在此基础上把 9×9 的和值运算可视化成了「45 账本」。

需要澄清两处易混点：`core/lesson-player.js` 的推进段实际为六段 `intro → demo → guided → noteToFill → semiAuto → free`（源码注释仍写「五段式」，noteToFill 是后加的桥梁段，`_currentPhase` 枚举含 idle/done 共 8 值），是用于教学关逐步引导的状态机，不等同于三级提示；`core/reasoning-monitor.js` 则是独立的四层推理监控（L1 笔记频率 / L2 空间分布 / L3 笔记准确率 / L4 笔记-填数间隔），为提示决策提供数据。

### 3.4 难度评级

星级公式（`getRating`）：`maxLevel×100 + remainingFactor×100 + densityFactor×50`，按剩余空格比例与「非孤星技巧密度」修正，最终映射为 1★–5★。公式属实，但分值区间并非 0–1000：全孤星题可为负（约 -50），Swordfish + 高剩余 + 高密度可达约 1200；星级阈值实际为 <250 / <400 / <525 / <600（`tech-rater.js` L2756–2765）。

### 3.5 持久化

`core/data-store.js` 实际分 **10 类**存储（progress / settings / achievement / learning / stats / story / gallery / seal / cache / ink_text；源码头部注释误写「九大类」），采用「分类:键」命名与双备份轮转 + 校验和恢复，并兼容旧版 key 迁移。

### 3.6 引擎配套模块与已发现缺陷

引擎侧配套模块职责（静态核实）：`core/solver-aware-acceptance.js` 是关卡验收门，跑 `TechRater.solve` 算 G1/G2/G3 三档指标，`CAGE_REASONING_TECHS=['cageUnique','rule45']`；`cage-analyzer.js` 用 OPEN/NARROWED/RESOLVED 三态回溯枚举笼指派；`cage-analyzer-report.js` 出关卡报告（初始三态 / 里程碑 / 关键路径 / 难度分）；`strategy-selector.js` 把戏剧导演意图映射为 AI 旋钮钳制；`strategy-replay.js` 把 TechRater 最优路径转为评估与提示动作对比。

新增引擎缺陷与技术债：`core/engine-adapter.js` 与数独无关注——它是对 `/api/v1/engine/judge` 的 HTTP 映射（字段含 current_word / user_answer / correct_answer），属疑似遗留/错位代码。`tech-rater.js` 全文件约 3394 行，`tech-rater.js.bak-hiddenpair` 是旧备份。求解器构造只把格子映射到最外层笼（`tech-rater.js` L187），嵌套/多笼格在求解器内被忽略，而 board.js 已支持 `cageIds` 多笼，存在能力错位。

---

## 四、Boss 战系统

### 4.1 主线规则：三点连线（TPL）

`game.html` 的 `startBossBattle` 明确注释：**三点连线（ThreePointLineManager，经 `tpl-battle-controller.js`）是主线 Boss 战唯一核心**，替代了旧 `BattleManager` 的 Focus / Parry / Interception / 忍杀等机制。旧的 `battle-manager.js`（约 23 万字符）仍保留在仓库中，形成事实上的双系统并存，是未来重构与维护的负担。

三点连线规则：玩家与 AI 在同盘竞速，各自「永久归属」填对的格子；盘面设置 3 个据点，其中「城堡」开局可见，另 2 个隐藏，玩家填对其核心格后才显现。据点争夺沿行 / 列 / 宫 / 笼四维进行，一方在某一维度填对数过半即永久占下该维度，一据点累计占领 3 个维度即归其所有；2:2 平局时据点迁移到干净位置，无干净位置则迁移失败。

### 4.2 胜利路径

绝杀连线、全局解题、强制结算三条路径。**绝杀判据需修正**：代码实为「棋盘满 + 一方据点数领先于对方」即可绝杀（`three-point-line-manager.js` `_checkBoardFull`），并非旧口径的「占满 3 据点后连线」；全局解题按归属格数决胜负（平看失误）；据点迁移失败且满盘无人达成则按据点→失误→归属格→平局强制结算（`_forceSettle`）。绝杀可点按钮触发，也可拖拽划过全部可见核心格触发并播放全屏三角爆发演出。

### 4.3 AI 对手与动态难度

AI 解题基于 TechRater 逐级找可见解（`_findAllVisibleResults`），人格决定技术上限、发现率、失误率、真假笔记与盲猜概率。动态难度三处均已落实在 `core/battle-manager.js`：按玩家均耗时调 AI 速率（`_adjustDifficulty`）、按领先/落后与连对/连错调失误率（`_calcDynamicErrorRate`）、调走子间隔（`_calcDynamicInterval`）；注意 `core/dynamic-threshold.js` 实为角色提示触发阈值，与 Boss 动态难度无关。戏剧导演（`director.js`）四阶段 opening / development / crisis / climax 与七类策略（施压/抢据点/陷阱/收尾等）已实现，主流程在 `game.html` 显式传 `directorShadow: false` 激活。

七章 Boss 映射（来源：`BOSS_LEVEL_MAP` + `BOSS_CONFIGS`）：

| 关卡 | 章 | 对手 | 人格画像 |
|---|---|---|---|
| 109 | 信 | 薇拉 | blind，低技巧高失误，试探布局 |
| 209 | 屋 | 伊藤 | 冷静观察 |
| 309 | 门 | 伊藤 | 暗门布题 |
| 409 | 绳 | 伊藤 | 补全终题 |
| 509 | 电 | 山田 | 测向车布控、紧逼 |
| 609 | 网 | 山田 | 追查压迫 |
| 709 | 库房 | 伊藤 | 终局现身 |

`content/boss-content.js` 提供 9 个内容包（含中/日/英台词与四阶段语料），与上述对手做映射。Boss 战成就覆盖「三点连线绝杀 / 全局解题 / 僵局裁决」三种。

---

## 五、UI / 交互 / 玩家功能

### 5.1 操作

键盘快捷键已核实（`game.html` 键盘处理）：`1–9` 填数、`N` 笔记、`Backspace`/`Delete` 擦除、`B` 书签、`W` 或 `D` 假设抽屉、`S` 快照、`A` 或 `Enter` 采纳、`1/2/3` 回退快照（抽屉拉开时）、`Esc` 逐级取消、过关弹窗 `Enter` 下一关 / `Esc` 关闭。长按数字键锁定「数字笔」连续填数，拖拽划选进入批量笔记模式。

### 5.2 假设模式（WhatIf）

`410` 教学关完成前禁用；最多 3 张快照；填入的数字以紫色区分且不消耗主盘；可逐步撤销、回退到指定快照或整体采纳。这是「任务压力关前安全试错」的关键设计。

### 5.3 辅助功能

热力图（难度热区 + Boss 影响力）、雪崩自动补全、45 账本（仅 9×9）、三级提示动画（支持到 X-Wing / Swordfish 双区域连线）、调查面板（证据 / 时间线 / 关系图）、成就面板（Boss 战成就）、连击系统（3/5/8 里程碑）、图鉴（角色立绘 / 档案碎片 / 道具 / 周目继承）、自由模式（随机 / 计时 / 每日种子 / 无尽）。

### 5.4 设置与布局

设置面板含音量四路（主/音效/语音/BGM）、三档画质、笔记模式、四语、自动候选笔记、跳过教学、难度热区、提示讲解员（沈墨/苏晚/薇拉/伊藤）、chibi 讲解、总静音。PC 与移动端布局由 `pc-layout-manager.js` 区分（宽≥900px 且宽高比≥1.4 时双栏）。

---

## 六、剧情、角色与本地化

### 6.1 剧情与角色

主角沈墨，1941 年珍珠港事变前夕的上海租界文书，循匿名数独来信追查父辈情报谜题链。七章依次为信·初识笼中密码、屋·藏书楼地下、门·藏书楼深层、绳·东余杭路94号、电·发报机、网·上海、库房·终局。核心角色含沈墨、苏晚、薇拉、伊藤、山田、潘汉年、周太太、父亲沈世安与回忆中的老师，终点在码头闭环。

叙事由 `story/story-engine.js` 打字机引擎驱动（情绪定速、立绘、背景淡入、CG、音效），`story-orchestrator.js` 编排序章 / 关卡前后对白 / 终章与首次技巧教学。

### 6.2 四语现状

支持 zh-CN / en-US / ja-JP / ko-KR。内部审计结论：日、韩已与中文对齐；**英文是唯一仍未对齐的语言（缺第二章 201–209 与终章）**。术语对照表已建立（笼=Cage、孤星=Lone Star、星衡法则=Star Balance Law、并蒂锁=Twin Lock 等）。

### 6.3 语音与资源

配音暂缓（`VOICE_ENABLED=false`），仅保留打字机「哒哒」音效。立绘中 teacher 仍缺、暂复用沈墨占位；第一章 CG 7 张中 03/05 已修复。

---

## 七、数据、校验、测试与工程

关卡校验 `scripts/validate-levels.js` 校验 JSON、尺寸、盘面、解、笼子、教学计划。本次为静态核实（未运行脚本）：63 关中缺 lessonPlan 的关卡为 15（`"lessonPlan"` 命中 48 文件），与「15 关缺 lessonPlan」吻合；但「14 关 guided.hintText 为空」实为**缺 `hintText` 键**（如 208/506/606 的 guided 只有 methodText，`validate-levels.js` L220 用 `!g.hintText` 告警），并非空字符串，精确值 14 未能逐关复算，且 `docs/lessonPlan-教学目标达标审核-2026-08-15.md` 另称「guided 双空已清零」，口径冲突、存疑。章节弧线校验（`config/chapter-arc.json` + `scripts/validate-chapter-arc.js`，`npm run validate:arc`）卡住章节→技巧→叙事钩子→尺寸契约，含 lessonPlan 弱引导质量门与三层语义门；但 **109/209 文案问题未完全修复**：209 已干净，109 的 `teachingGoal` 已改「21法则」，trigger 仍「10法则」、preDialog 仍「十则」、prerequisite 仍 `rule_of_ten`，而 arc 校验只查 teachingGoal 不查这几处，残留会逃过校验。

AI 测试覆盖 solver 可解性、自由求解（挂接 10 技巧）、LLM 三人格教学测试，共 14 个脚本 400+ 断言；但有 2 个陈旧测试失败（`test-battle-manager.js` 41/2、`test-battle-e2e.mjs` 引用了已删除的 level-801）。

特殊玩法字段并非未启用——静态核实实际关卡：208/506 启用 extract、505 启用 plantedErrors；604（上限 3）与 606（上限 2）启用的字段实为 **`limits.maxErrors`**（此前报告写作 errorLimit）；603/605/609 启用的字段实为 **`silentMode: true`**（此前写作 silentRadio，全库无此字段名）。此前旧文档说「这些字段仅代码预留」是不准确的。

发布就绪评估总体 **78%**（玩法 95% / i18n 100% / 资源 62% / 工程 72%）。PM 交接评估（2026-08-15）结论：具备可玩内测版条件（约 85%），正式版唯一真正 P0 是**前端构建/打包系统缺失**（约 1–2 周补上即可内测、2–3 周正式版）。

---

## 八、发现的问题与风险（按优先级）

**P0（必须解决才能正式发布）**

1. **无前端构建/打包系统**。当前靠本地静态服务器跑原生 ESM，没有产物化、压缩、资源哈希与发布流程。`scripts/build-release.cjs` 只是 B3-FINAL 关卡池的领域专用打包器（且未注册进 package.json），不能替代前端构建。
2. **语音（VO）后置**。语音是资源维度最大缺口，旧角色语音待替换，且需真人参考音与授权方案。

**P1（高，建议发布前处理）**

3. **二/三周目路线（「三岔口」设定）未落地**，level-801 有剧本无盘面；`scripts/test-battle-e2e.mjs` 与 `test-battle-manager.js` 仍残留 801 及旧 8 章断言，会导致测试失败。剧情设定与关卡实现错位。
4. **英文四语未对齐**（缺 201–209 与终章文案），其余三语已对齐。
5. **旧/新 Boss 战系统强耦合并存**。`battle-manager.js`（约 226 KB / 6400+ 行）保留 Focus/Parry/Interception/忍杀等废弃机制；新系统 `tpl-battle-controller.js` 反而依赖旧系统的 `AIPlayerCore` 与 `BOSS_CONFIGS`，极易误导维护者。
6. **文档漂移**。部分手册/文档仍写「8 章」、旧 Boss 机制或旧关卡口径；`游戏说明书.md`（v3.7）需据此报告更新。
7. **关卡校验警告未清（15 关缺 lessonPlan + 14 关缺 hintText 键）**；**109 文案问题未修干净**（trigger/preDialog/prerequisite 仍残留「10法则/十则/rule_of_ten」，arc 校验只查 teachingGoal 会漏）；209 已修复。arc 校验本身可继续承载，但需扩展检查项。
8. **Boss 台词与教学文案存在硬编码中文**，四语数据已备但未被消费。
9. **isBoss 双口径漂移**：`isBoss:true` 出现于 13 处（109/208/209/307/309/406/409/506/509/606/609/706/709），而 `config/chapter-arc.json` 只定义 7 个 boss（109/209/309/409/509/609/709）；多出的 208/307/406/506/606/706 恰是 `test-battle-e2e.mjs` 的旧 boss 映射，两套约定并存。

**P2（中）**

10. **假设模式（WhatIf）解锁门控锚定在并不存在的「关卡 410」**。`game.html` L4027–4028 以 `currentLevelId === 410` / `isLevelCompleted(410)`、`ui/main.js` L896 另以 `_currentLevelId === 410` 豁免判定，但 `data/levels/` 只有 401–409，没有 `level-410.json`。按当前代码，假设模式很可能始终锁定（410 豁免分支也永远不触发）。中度玩家功能缺陷。
11. `perspectiveMode` 未完整接线；`backend-engine/` 属其他项目（BestBay），容易误读；`core/engine-adapter.js` 也是对 `/api/v1/engine/judge` 的 HTTP 映射（字段含 current_word 等），属疑似遗留/错位代码。
12. **绝杀判定口径内部漂移**：`three-point-line-manager.js` 已放宽为「棋盘满 + 领先」绝杀，但头注与 `tpl-battle-controller.js` 注释仍写「占满 3 据点」；`_aiMove` 判断连线用归属格数、与 `canLineWin`（按据点数）口径不一致。
13. 错误日志缺失；无 CI，`game.html` 单文件 8000+ 行有技术债。

**P3（低）**

14. `guess` 试数已声明但未启用，若未来出现无纯逻辑解的关卡需评估是否补兜底。
15. 4×4 优先级不含 cageUnique/rule45，无法做笼和推理（与 board.js 的 cageIds 多笼能力错位）；`tech-rater.js` 构造只认最外层笼、忽略嵌套/多笼格。
16. teacher 立绘占位；部分命名（WhatIf / 假设 / 三岔口）术语不一致；`reasoning-monitor.js` 与 `dynamic-threshold.js` 只挂 `window.*`、无 ES module export，与「纯 ES Modules」约定不一致。

---

## 九、改进建议（摘要）

优先补齐前端构建打包与语音方案；统一两套 Boss 约定（chapter-arc 与 test-battle-e2e 的 boss 映射、isBoss 标记）并清掉 level-801/旧 8 章残留；把 109 的和值文案三处残留扫干净、扩展 `validate-chapter-arc.js` 检查 trigger/preDialog/prerequisite；把旧 `battle-manager.js` 的废弃机制与 `AIPlayerCore`/`BOSS_CONFIGS` 解耦或归档，明确三点连线为唯一主线并统一绝杀判定口径；清空 29 条关卡校验警告（含缺 lessonPlan 与缺 hintText 键），修正 `engine-adapter.js` 错位代码与求解器多笼不一致；最后引入最小 CI 做关卡校验、arc 校验与 `node --check` 语法检查。

TechRater / CageGenerator 方向：给 TechRater 补上唯一解计数与多格 Rule45、为 find 系列加候选 snapshot/restore 防污染，修掉 `getRating` 的 manual 崩溃与 `ai-tech-verify` 证据 schema 漂移；生成器侧把 v9 链模式的硬编码 `guidedInfo` 换成真实求解器证据，并把「目标技巧必要性」立为链模式验收口径；把 26 个研究脚本、release gate 收口进最小 CI，统一 top4/ratio 阈值、消除双闸门口径。

---

## 十、TechRater 与 CageGenerator 深审（专项）

> 本次对两大核心资产做静态深审：求解器 TechRater、关笼生成器 CageGenerator（v8/v9）及其流水线。以下问题与 §8 同一优先级体系，集中列于本节。

### 10.1 TechRater 求解器

`core/tech-rater.js` 全文件 3394 行（IIFE）。核心入口：构造函数 L151–217、`solve` L391–494、`_findAllByTechnique` L760–801、各技巧 `_find*`（nakedSingle 811 → swordfish 2383）、`_fillCell` L2649、`getRating` L2693–2796、`getTriPhaseScript` L2809、`applyMove` L2986、`exportLevelReport` L3301。

对照《TechRater改进方向分析-2026-08-09.md》的落地情况：已落地 #9 `teachingBreakthroughScore`、#10 删除 xwing/swordfish 缺证据时的硬编码回退；部分落地 #2（nakedPair/hiddenPair 已补证据但未做 snapshot/restore）、#4（定向提示在 `expert/hint-system.js` 而非本文件）、#7（`_findAllCombos` 加 `canPlace` 剪枝，但 `_getPossibleNumbers`/`_combosHelper` 仍无剪枝无 memo）；未落地 #1 回溯/唯一解计数、#3 必要技巧集（`getRating` 仍 `baseScore=maxLevel*100`）、#5 多格 Outie/Innie、#6 `countSolutions`、#8 权重、#11 `stuckReason`。

边界已确认：rule45 只做**单格** Innie/Outie（`outsideEmpty.length===1` / `innieEmpty.length===1`），无多格、无跨宫组合；笼和 cageUnique 用升序位掩码枚举去重。求解器构造只认最外层笼（`cellCage` 首笼胜出），嵌套/多笼仅初态约束与 cageUnique 生效、求解中不重算。

新增缺陷：`battle-manager.js` 长期复用同一个 rater 实例反复调 `_findAllByTechnique`，而 nakedPair/hiddenPair/nakedTriplet/pointingClaiming 均就地改 `this.candidates` 且无 restore，长期持有会累积污染候选（hint-system 每次 new 新实例则无此问题）；rule45 取相交笼和时用 `cellCage` 只取首笼，嵌套笼可能漏算/双计；`_findAllCombos` 是「数字集合枚举」而非「格子指派枚举」，`canPlace` 未约束一格一数；`getRating` 遇 `manual` 步会因 `TECHNIQUES['manual']` 未定义而抛 TypeError；`ai-tech-verify.js` 的证据 schema 与实现漂移（rule45 期望 `inwardValue/outwardValue/diff`，实现为 `subtype/sumOutsideValues`；pointingClaiming 期望 `scopeType/scopeIndex/eliminatedCells`，实现为 `direction/boxIndex/eliminated`），导致证据校验误判缺失。

### 10.2 CageGenerator 生成器

`scripts/cage-generator-v8.cjs` 共 3087 行（自报 CageFixer v7），主线七步：完整解→划笼→算和→三幕锚点→逆序挖洞→TechRater 难度→纯净度，导出 `{CageFixer, toV4Level}`；v8 其实已有 X-Wing/Swordfish 探测与引导挖洞，但 `injectTargetStructure`（L977 定义）全文件无调用，属死代码。

`scripts/cage-generator-v9.cjs` 共 3236 行，自报 v9，主线是「候选集注入法构造 X-Wing/Swordfish + 多技巧链验证 + 视觉巧思」。相对 v8 的真正增量是：候选结构预生成（`buildCandidateStructure`）、技巧链 `_digWithChain`、拓扑评分/形状多样性/成长偏差（默认 growthBias=0.4、topologyWeight=25、shapeDiversityWeight=0.2）、基于 fitness 的客观择优、`_validateAesthetics` 视觉校验；并保留 v8 的 `_digAndTune` 作非链回退。导出仅 `{CageFixer}`。因此此前「v8=逆序挖洞、v9=高阶技巧注入」是一句不准确的简化，应改为「v8 已有技巧注入雏形、v9 用候选集+链+拓扑客观择优重写并保留旧回退」。

`tests/cage_generator/` 下 6 个 JSON 是冻结回归基线：`baseline.json`（v0，`shipped_9x9_levels:48`）与 `audit_snapshot.json`（v0，`totalLevels:48`）为早期快照；四个 `baseline-v9-*` 带 verdict/supersedes 链。48→50 漂移可证：当前 `data/levels/` 中 `gridSize:9` 实为 50 关，而基线仍写 48（新增哪两关无法仅凭静态文件定位）。

新增缺陷：v9 技巧链模式 `_generateOne` 的 `guidedInfo` 为硬编码字面量 `{baseUnsolved:false, fullSolved:true, techUsedInFull:1, totalTechCount:{chain:1}}`（L2418–2424），非真实求解器输出；默认链模式不保证「目标技巧必要」，仅 `strictAdvanced` 才做「无此技巧不可解」且实测 0/20、默认关闭；拓扑/形状阈值大量硬编码（如 `-0.4*top4Share`、`(canonicalShare-0.45)*5`、`CANONICAL_SHAPE_SET=['L','T','domino','straight']`、`targetSteps=75`、`minAvalancheSize=12`）；随机源 mulberry32，默认 `seed=Date.now()`（+attempt/+i），默认批次不可复现；v8 的 `injectTargetStructure` 与两个 find 函数为无调用死代码。

### 10.3 生成器流水线与数据

B3→发布链路闭环：`docs/cage-b3a-objective.md` → `cage-generator-audit.md` 冻结配置 → `data/b3-final-regression.json` 7/7 全 true → `generate-release-pool.cjs` → `release/B3-FINAL/manifest.json` 记为 PASS。B4 则未落进 v9：`docs/B4/README.md` 声明全部 soft 杠杆已排除、实验闭环，子分支 A–G 全部 ❌/CLOSED/FROZEN，v9 只留下默认关闭的测量钩子。

`package.json` 只有 migrate/test:ai/validate/validate:arc 四条命令，26 个研究脚本（12 个 `b3*.cjs` + 14 个 `b4*.cjs`）与 cage-audit/generate-pool/build-release 等均须手工 `node` 触发，无 npm 入口；`cage-generator-v8.cjs` 进程内仅有自身 usage 引用，另有 31 处统一 `require('./cage-generator-v9.cjs')`。

`data/release-pool-b3final.json`（100 个 GEN-xxx 生成关卡）与 `data/levels`（63 个剧情关卡）是两套不同资产。**双闸门口径漂移（P1）**：`release-pool-b3final.json` 内置 gate 有 2/7 未过（top4 52.5% 越 53% 线、ratio 21.0% 低于 baseline 23.3%），但 `release-acceptance.cjs` 产物报告改用 ±1pp/−3pp 宽松口径后判 7 项全过、manifest 记为 PASS；两套阈值未声明为不同口径。

其它流水线问题：v0 审计基线（48 关、47/48 为 1 星）冻结于 08-09，与当前 63/50 关脱节未刷新；`experiments/B4/active` 仍写「当前 B4-A」而分支已 FROZEN、`archived` 写「空」但已有 A/E/G 结论、`config/research/B4/B4-C.json` 仍 todo 而日志已 CLOSED，且 config 只覆盖 A/B/C/D、缺 E/F/G；审计器职责重叠（cage-audit v0、cage-topology-entropy B3-B0、b4f0/f1/f2），`acceptance-gate-result.json` 是 1264 行逐字重复的复采样假象数据（seed 不推进 bug）、已被 `cage-density-forensics.json` 取代却仍留库。