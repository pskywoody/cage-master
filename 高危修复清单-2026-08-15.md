# 高危项修复清单（TechRater / CageGenerator）

> 来源：《项目审核报告-2026-08-15.md》第十节。
> 方法：源码静态审计（未运行脚本、未修改源码）。
> 范围：仅高危三项，建议按 H-1 → H-2 → H-3 顺序修复。

---

## H-1 CageGenerator v9 链模式验收造假

**问题**：v9 技巧链模式产出的 `guidedInfo` 是硬编码字面量，并非真实求解器结果；默认链模式也不验证「目标技巧真的必要」。生成器「产出必须用 X-Wing / Swordfish 才能解的进阶关」这一核心承诺目前没有工程证据支撑。

**证据**：
- `scripts/cage-generator-v9.cjs` L2418–2424：`guidedInfo` 恒为 `{ baseUnsolved:false, fullSolved:true, techUsedInFull:1, totalTechCount:{ chain:1 } }`。
- `_digWithChain` 默认只验 `fullTechList` 可解（L2607–2640），不验基础技巧集是否解不出、不验目标技巧是否真被使用。
- `strictAdvanced`（L2650–2661）才做「无此技巧不可解」校验，代码注释自述成功率 0/20、默认关闭。

**修复步骤**：

1. 生成关卡后用真实 `TechRater` 求解一次，据实测填入 `guidedInfo`：`baseUnsolved`（仅基础技巧是否解不出）、`fullSolved`（全技巧是否解出）、`techUsedInFull`（目标技巧是否实际被使用）、`totalTechCount`（实际技巧分布）。
2. 链模式默认验收口径改为三条件同时成立：基础集解不出 + 全技巧解出 + 目标技巧被使用；不能只判「可解」。
3. `strictAdvanced` 若继续保留，成功率低时不得用默认关闭掩盖，应如实标注该模式为实验性并把失败样本写入审计结果。
4. 删除硬编码 `guidedInfo`，避免误导任何下游读取方。

**验证**：抽取一批生成关跑真实求解器，断言 `guidedInfo` 数值随关卡变化且与求解路径一致；单测断言链模式下目标技巧确实出现在解题路径。

---

## H-2 发布池双闸门口径

**问题**：`release-pool-b3final.json` 内置 gate 有 2/7 失败，但 `release-acceptance.cjs` 产物报告用更宽松口径判 7 项全过、`manifest.json` 记为 PASS。两套阈值并存且未声明为不同口径，发布物「自检失败却对外标通过」。

**证据**：
- `data/release-pool-b3final.json` 内置 gates：`top4_50_53:false`（实测约 52.5% 越 53% 线）、`ratio_not_below_baseline:false`（约 21.0% 低于 baseline 23.3%），另 5 项 true。
- `scripts/release-acceptance.cjs` 产物报告改用 ±1pp / −3pp 容差后判全过；`release/B3-FINAL/manifest.json` 记 PASS。

**修复步骤**：

1. 确定唯一验收源：release 脚本与 manifest 统一从 `release-pool-b3final.json` 的内置 gates 读取判定，不再在报告里另设隐式宽松口径。
2. 若业务上确实接受 ±1pp / −3pp 容差，把该容差写进 gate 定义与文档，并让内置 gates 用同一阈值计算，而不是报告层重新解释。
3. 当前 2 个失败项如实暴露：未过则 manifest 标 FAIL / 警示，禁止静默 PASS。
4. 把「判定逻辑」收敛到单一函数，供 release-acceptance、manifest、后续 CI 三处复用。

**验证**：重新执行 release 流程，断言 manifest 的 PASS/FAIL 与内置 gates 输出完全一致；写脚本对同一 pool 分别调用两套逻辑，断言结果一致。

---

## H-3 battle-manager 长持有 rater + find 函数污染候选

**问题**：`battle-manager` 长期持有一个 `TechRater` 实例并反复调用 `_findAllByTechnique`，而多个 find 函数会就地修改 `this.candidates` 且不恢复，导致 Boss 对局中候选随调用累积漂移，可能污染 AI 走向与提示正确性。

**证据**：
- `core/battle-manager.js` `_initRater`（约 L1699）长期持有 `this._rater = fromBoard(board)`；`useGuanJu` / `useDingShi`（约 L2057 / L2076）反复调用。
- `core/tech-rater.js` 中 `_findNakedPair`（约 L1477）、`_findHiddenPair`（约 L1655）、`_findNakedTriplet`（约 L1982）、`_findPointingClaiming`（约 L1789）就地改 `this.candidates`，无 snapshot / restore。
- 对照：`expert/hint-system.js` 每次 `new` 新实例，无此问题。

**修复步骤**：

1. 治本：在上述 find 函数入口快照本次需要修改的候选状态、结束时恢复，或改为返回「差异结果」而不改原状态。
2. 治标（可选并行）：`battle-manager` 每次推理前从当前 board 重建 rater，避免长生命周期状态污染。
3. 为 `_findNakedPair` 宫分支补上统一的返回结构（当前 L1604 直接 `return result.nakedSingleResult`，裸对象缺 technique/evidence）。
4. 清理 `_fillCell` / `applyMove` 以外任何会残留副作用的技巧函数。

**验证**：写回归测试，对同一盘面连续调用 `_findAllByTechnique` 两次，断言第二次候选集与第一次一致；跑 Boss 长对局 smoke，观察 AI 候选不随时间漂移。

---

## 完成口径（三项通用的验收门）

- 每个修复提交需附一个能复现/校验该缺陷的最小测试或脚本。
- 高危项修复后再更新《项目审核报告-2026-08-15.md》第十节，把这三种问题从「高危」降级或关闭。
- 全程不删除用户数据、不永久覆盖文件；改代码前先确认目标文件在版本控制内或已备份。